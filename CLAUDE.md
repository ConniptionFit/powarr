# Powarr — Master Prompt (LLM IDE Agent)

> **This is the authoritative agent context for Powarr.** Use this whole file as a prefix, together with the linked vault notes, before any future additions to the app. Supersedes [[LLM Context]] (legacy stub). Canonical copy: `Powarr/Master Prompt.md` in the Obsidian vault — keep this mirror identical. Last updated: 2026-07-19 (v0.84.0 — **UI polish & accessibility pass** (from a full-tree UI deep-dive review the user requested; no behavior/scoring changes — refinements only): global `:focus-visible` brand-light outline in `frontend/src/index.css` (keyboard/AT users had almost no visible focus indicator — matters now that Match Review/Deletion Suggestions have j/k/x triage; fires on keyboard focus only, not mouse); `aria-label`s on icon-only controls (Discovery accept/reject, modal close buttons); the last three bare "Loading…" placeholders → `Skeleton` (Settings scoring weights, Integrations list, Match Review "why not auto-import" detail); Settings gained a direct category-switcher pill row (jump between categories, no bounce back to the grid) and an unknown `/settings/:category` now redirects to the grid instead of rendering an empty heading; `components/Breadcrumb.tsx` became a real clickable `Area › Screen` trail (was a static label); Overview's primary "Sync Library" button uses the brand color (was `yellow-700`, which reads as caution); `AreaTabs` active border uses the `brand-light` token (was off-palette `purple-500`). **Two review items deliberately deferred as isolated, browser-verified follow-ups** (regression-prone, zero visual change): **FE-01** split the ~2.4k-line `Settings/index.tsx` into `pages/Settings/sections/`, **FE-02** normalize route-content padding — both new Low To-Do rows in [[Future Improvements]]. 828 tests pass (no backend logic changed); `npm run build` clean; verified live post-deploy — deployed CSS carries the `focus-visible{outline:2px solid #a78bfa` rule and the JS bundle carries `v0.84.0`/`aria-label`/the category switcher. Prior: v0.83.0 — **AD-22 Recently Added section (user request)**: Music → Artist Discovery gained a collapsible **Recently added** section (open by default, Last 10/25/50 select) listing the last N artists actually added to Lidarr with thumbnail, short bio, when, and why. Read-only `GET /artist-discovery/recent-adds?limit=` backed by `services/artist_discovery.py::list_recent_adds()` — reads `ArtistAddLog` (v0.50.0, genuine adds only) newest first, joins discovery adds back to their accepted `DiscoveredArtist` row (mbid preferred, artist-name fallback — both paths confirmed live) for why-suggested fields/image/bio, backfills missing images from the AD-21 `artist_thumbnails` cache (covers AD-08's enrichment-image purge since a just-added artist is in the library); Related Artists adds have no candidate row — reasoning is just their source. Frontend reuses the shared `ArtistCard` shell (added-time in the `actions` slot) and the candidate cards' `whySuggested()` wording (param loosened to a structural Pick). 828 tests pass (7 new in test_recent_adds.py), in-container too; verified live post-deploy — `/recent-adds?limit=3` returned real production rows (graph-lane seed names, Deezer thumbnails, one name-fallback match) and the bundle carries the section + v0.83.0. Prior: v0.82.0 — **UX-06 Deletion Suggestions keyboard triage + OPS-03 backup staleness awareness**: second consecutive Feature-Ideas promotion session (the To-Do table still holds only user-decision/conditional rows — OPS-01, SEC-HTTPS, AUTH-01, AD-01 — so the actionable backlog is genuinely empty; future feature work needs new user requests or another explicit promotion pass). **UX-06** (the A11y idea's "extend shortcuts to Deletion Suggestions" remainder): same listener pattern as UX-05 — `j`/`k` move a ring focus over the visible rows (show groups in By Show mode, single items otherwise), `x` select, `d` opens the LIB-01 **non-destructive** deletion preview (its Confirm stays click-only — no key ever deletes anything directly, same destructive-stays-click-only rule as UX-05), `i` toggle ignore, `Esc` clears; keys ignored while typing/with modifiers/while a modal is open; the `Kbd` keycap factored out of MatchReview into shared `components/Kbd.tsx`. **OPS-03** (the alerts-on-staleness slice of the Smart Backup Strategy idea): read-only `GET /settings/backup/status` — `services/backup.py::backup_status()` checks only local backup files + the naive-UTC `last_backup` AppSetting (newest-file-mtime fallback), stale = enabled and age > **2× interval** (grace against one late maintenance tick) or never-completed; amber Overview banner renders only when stale. The idea's auto-frequency/S3-retention halves deliberately stayed brainstorm (behavior changes, not observation). 821 tests pass (8 new in test_backup.py), in-container too; verified live post-deploy — `/settings/backup/status` returned the production instance's real healthy state (enabled, last backup 10.1h ago, 7 files, not stale) and the bundle carries the row ids/shortcut hint/banner/v0.82.0. Prior: v0.81.0 — **LIB-06 library health dashboard + UX-05 Match Review keyboard shortcuts**: with the To-Do table holding only user-decision/conditional rows (OPS-01, SEC-HTTPS, AUTH-01, AD-01), that session promoted two Feature Ideas via the documented promotion workflow. **LIB-06**: read-only `GET /media/health` backed by `services/library_health.py::compute_library_health()` — per-type footprint, *arr link coverage (movie/episode/track only, the types `link_arr_ids()` actually links), duplicate load (reuses LIB-03's grouper wholesale), AD-21 thumbnail-cache coverage, open failed-import backlog + malformed flags, protect-flag shielding, pending soft-deletes — computed from **synced local tables only** (no live Plex/*arr calls, can never hang on a flaky integration) and deliberately **no composite 0-100 health score** (score formulas are a confirmation-gated surface; each KPI tile on the new **Library → Health** page links to the actionable page instead). **UX-05**: keyboard triage on Match Review — `j`/`k` move a ring-highlighted focus through the filtered/sorted list, `x` select, `e` expand, `y` accept (two-step, same Confirm Import the button shows), `n` reject (reject-and-remove-download deliberately stays click-only), `Esc` backs out; keys ignored while typing or with modifiers. Also: the [[Future Improvements]] Complete-table **housekeeping pass** its own notes called for — fragmented single-row tables consolidated, v0.73.0-and-older entries moved **verbatim to a new Archive section** (module notes' "full version-by-version history" references resolve there; keep that split going forward), and [[Powarr Overview]]'s Pages table de-drifted (still listed the pre-v0.43.0 Import Queue tab; missed Duplicates/Recent Downloads/LLM Accuracy/Related Artists). 813 tests pass (8 new), in-container too; verified live post-deploy — `/media/health` returns real KPIs against the production DB (104k tracks, 92% thumbnail coverage, 9 duplicate groups), deployed bundle carries the Health route + shortcut hint. Prior: v0.80.0 — **AD-21 library-artist thumbnail cache (user-reported)**: the Related Artists typeahead never showed artist photos — its thumbnails came from Last.fm's `artist.search` images, which have been blank/placeholder for years. Powarr now **keeps its own thumbnail URL for every artist actually in the library**: new `artist_thumbnails` table + `services/artist_thumbnails.py` (Lidarr `remoteUrl` poster first, Deezer name-search fallback for Plex-only artists, 250 lookups/run cap, misses retried after 7 days), refreshed by a new daily zero-config scheduler job (`last_artist_thumb_refresh` gate, standard due-ness pattern) that also **deletes rows once an artist leaves the library** — the refresh aborts entirely (cleanup included) when Lidarr is unreachable so a flaky integration never reads as mass deletion, and never deletes on an empty library set. `search_artist_names()` overlays the local URLs onto typeahead results; `_lastfm_image()` now rejects Last.fm's known placeholder star. **Live-verification catch worth remembering**: Lidarr sometimes reports its container-local `/config/MediaCover/...` cache path as `remoteUrl` (596 of 885 first-run rows) — only http(s) URLs are accepted now, and bad rows self-heal on the next refresh. Verified live end-to-end: 0 bad URLs after the fix, `search-names?q=aesop rock` returns the library artist's real Lidarr poster URL. 805 tests pass (13 new), in-container too. Prior: v0.79.0 — **AD-18 preview rework (user-reported)**: the preview Play button rendered unconditionally and, with no source configured, every click dead-ended in "No preview available." Now it **only renders when a preview is confirmed**: `ArtistPreviewButton` checks availability when its card scrolls into view (IntersectionObserver, so a 50-card Related Artists page doesn't fire 50 YouTube searches up front) and renders nothing on no-sources/no-match/failure; click reveals the already-confirmed player. This retires AD-18's original "never automatic, one call per click" contract by user request — the compensating quota control is an in-process cache in `artist_preview.py` (24h hits / 1h misses, keyed on normalized artist + enabled-source fingerprint, `clear_preview_cache()` test hook). Tooltip de-jargoned (no backlog IDs in UI text); enable checkboxes relabeled "Search YouTube/Spotify for artist previews" — Spotify's notes it's off by default (both already default-unchecked and already gated server-side; the labels just didn't say so). 792 tests pass (3 new), verified in-container too; verified live post-deploy (bundle greps + `preview?artist=` → `{"sources":[]}` → no button). Prior: v0.78.2 — **performance/cleanup session (log review + code audit, no backlog ID)**: live-log review found `_scheduled_playlist_generation` was the **only** `maintenance_loop()` job with no last-run due-ness gate — a full 30-40s generation ran on **every 5-minute scheduler tick** (observed live at ~5.5-min cadence, ~288 runs/day) against a configured 24h `schedule_interval_hours`; the setting existed since v0.35 but was never read by the scheduler, and [[Smart Playlists]] even documented a due-ness check that never existed in code. Now gated by a `last_playlist_generation` AppSetting timestamp, same pattern as every sibling job (`<=0` keeps every-tick behavior). Also: `run_differential_sync()` now skips no-op Qdrant payload writes — it was rewriting all ~285 points every hourly cycle unchanged; the run message reports `(M unchanged, skipped)`, so a steady-state hourly sync reporting ~0 synced is **correct**, not broken. Repo hygiene: committed the v0.78.1 CLAUDE.md docs sync a prior session left uncommitted, removed a stale detached worktree, and gitignored `.claude/` (untracked local session state incl. a node_modules-bearing worktree was one `git add -A` away from being committed by the hands-free deploy flow). Also backfilled the missing v0.78.1 row in [[Future Improvements]]'s Complete table. 789 tests pass (9 new); `npm run build` clean. Deployed by the user the same day; the scheduler gate verified live during the v0.79.0 session — **zero** scheduled-generation log lines in the 3h after the rebuild (was ~33 per 3h). Earlier: v0.78.1 — **live production incident, same-day fix**: scheduled Smart Playlists generation froze the shared asyncio event loop (and the healthcheck) for minutes — `playlist_generator.py::_add_artist_tracks_to_db()` re-materialized the entire 101k-row tracks table once per artist per group; fixed with a `_tracks_by_artist(db)` index built once per generation run. A CPU-bound/non-yielding bug class, distinct from the three prior held-transaction incidents (v0.74.1, twice in v0.76.0) — both look identical from `docker ps` alone; a live `py-spy dump` when CPU is pinned is how to tell them apart. Full incident writeup in [[Smart Playlists]]. Earlier: v0.78.0 **AD-18** listen-before-add preview (YouTube + Spotify, read-only, YouTube Music scoped out — no official API), v0.77.0 **INT-01** Bazarr subtitle orphan awareness, v0.76.0 **NOTIF-01** Discord webhook channel + a two-part held-transaction incident fix (`run_differential_sync()`/`run_full_discovery_cycle()` in `services/artist_discovery.py`). **Process notes for future sessions**: (1) this container's outbound connections to Sonarr/Radarr/Lidarr/Qdrant/Ollama occasionally spike concurrently (post-restart sync, or a scheduled LLM backlog run) — during these windows `docker ps` can legitimately flip to `unhealthy` for a few minutes; distinguish a genuine incident from this benign pattern by checking `pg_stat_activity` for stuck transactions **and**, when CPU stays pinned with no stuck transaction, consider a CPU-bound freeze (v0.78.1-style) rather than assuming it's benign just because the DB looks clean. (2) A real human user may be actively using the live app concurrently with an agent session — be mindful before triggering heavy on-demand operations purely for verification when live traffic is present. (3) `py-spy` isn't in the base image but installs cleanly via `pip3 install py-spy` in a running container; attaching to another process in the same container needs `SYS_PTRACE`, which the compose file doesn't grant by default — always get explicit user approval before adding it (correctly blocked once by the permission system when attempted without asking), and remove it again after diagnosis. Older entries (v0.39.0–v0.76.0: Artist Discovery, Smart Playlists, auto-import gate, SEC-A-01, Related Artists, SP-09 through SP-16, LIB-01 through LIB-05, FI-06 through FI-10, AD-17/19/20, LLM-05 through LLM-09, AN-01, INT-02, OPS-02, etc.) are covered in full in [[Future Improvements]]'s Complete table — trimmed from this header to keep it from growing indefinitely, per the Non-negotiable Principles' own pruning intent.

## Identity & Purpose

You are an LLM IDE agent responsible for reviewing, maintaining, and extending **Powarr** — a self-hosted FastAPI/React media-cleanup and failed-import-triage tool for the Plex + *arr stack. A good session: check reality against docs before trusting either, make a scoped change through exactly one capability module, verify it on the live container, and leave **both the Obsidian vault and GitHub** accurately reflecting what changed.

## Project Facts

| Field | Value |
|---|---|
| Version | v0.84.0 — **2026-07-19**. UI polish & accessibility pass (from a UI deep-dive review) — global keyboard focus ring, aria-labels on icon-only controls, Skeleton loaders replace the last bare "Loading…" text, Settings category switcher, clickable Area › Screen breadcrumb, brand-color primary Sync button; no behavior/scoring changes. Full changelog in the header note above; older entries live in [[Future Improvements]]'s Complete table and Archive section rather than growing this row forever — recent highlights: v0.83.0 AD-22 Recently Added section on Artist Discovery, v0.82.0 UX-06 Deletion Suggestions keyboard triage + OPS-03 backup staleness banner, v0.81.0 LIB-06 library health dashboard + UX-05 Match Review keyboard shortcuts, v0.80.0 AD-21 library-artist thumbnail cache, v0.79.0 AD-18 preview rework (button only renders on a confirmed preview), v0.78.2 perf/cleanup (scheduler due-ness gate + sync no-op skip, verified live), v0.78.1 live-incident fix (full-table-scan event-loop freeze), v0.78.0 AD-18 listen-before-add preview, v0.77.0 INT-01 Bazarr subtitle awareness, v0.76.0 NOTIF-01 Discord channel + two-part stuck-transaction fix, v0.75.0 SP-15/AD-19/LLM-05 mood/era tagging + mood lanes + readarr evidence, v0.74.x SP-16/AD-20 fixes + stuck-transaction incident, v0.73.0 SP-11 playlist-naming diversity, v0.72.0 OPS-02 config-as-code export/import, v0.71.0 INT-02 manual *arr ID override, v0.70.0 AN-01 import funnel analytics, v0.69.0 LLM-09 junk strip rules, v0.68.0 LLM-08 per-app/per-library policies, v0.67.0 LLM-07 deletion-score second opinions, v0.66.0 LLM-06 in-app accuracy dashboard, v0.65.0 SP-09 related-artist generation, v0.64.0 SP-13 prune stale tracks, v0.63.0 SP-12 playlist templates, v0.62.0 AD-17 recently-listened lane, v0.61.0 FI-10 malformed-import audit, v0.60.0 FI-09 recent-downloads tab, v0.59.0 FI-08 anime absolute-pack coverage, v0.58.0 FI-06 root-cause classifier, v0.57.0 LIB-03 duplicate hunter, v0.56.0 LIB-02 Sonarr episode delete modes. |
| Container | `powarr`, port `7979`, Docker host `10.1.1.2` (`ssh docker`, key auth) |
| Source of truth | Host repo `/mnt/ServerFiles/Docker/composeFiles/powarr` (= build context) |
| Local working clone | `~/Projects/powarr` on the Mac — **edit here**. Confirmed 2026-07-08: this is the current FastAPI/React app (`backend/`, `frontend/`, `CLAUDE.md`); `origin` = host repo, `github` = public mirror. The legacy Node.js iteration was archived to `~/Projects/powarr-legacy-node` (reference only, not deployed). |
| Remotes | `origin` = host repo (push-to-deploy: `receive.denyCurrentBranch=updateInstead`); `github` = public [ConniptionFit/powarr](https://github.com/ConniptionFit/powarr) (`gh` CLI authed) — renamed from the private `powarr-v2` 2026-07-08, confirmed live |
| Deploy | commit → `git push origin` → `ssh docker 'cd /mnt/ServerFiles/Docker/composeFiles/powarr && docker compose up --build -d'` → verify → `git push github` |
| Stack | Python 3.12 + FastAPI + SQLAlchemy; React 18 + TS + Tailwind; single multi-stage image; HEALTHCHECK on `/api/v1/system/health` |
| Database | PostgreSQL container `postgres` on `postgres_net` (db/user `powarr`); SQLite fallback |
| API | `/api/v1/*` (breaking changes → `/api/v2/`); docs at `/api/docs` |
| Tests | `docker exec powarr python -m unittest discover -s app/tests` (must pass before handoff). Local pre-deploy iteration: `cd ~/Projects/powarr/backend && POWARR_DATA_DIR=<writable> uv run --python 3.12 --with pydantic --with pydantic-settings --with sqlalchemy --with httpx --with fastapi python -m unittest discover -s app/tests` |
| Secrets | `docker-compose.yml` is **gitignored** (DB password inline — never commit); API keys/auth secrets live in DB rows |
| Old iteration | `~/Projects/powarr-legacy-node` is the archived legacy Node.js Powarr (reference only, not deployed, not the source of truth for anything). |
| Trap | `/Volumes/ServerFiles` SMB mount and the `10.1.1.x` LAN link flap — if unreachable, it's the Mac's network, not the server; git-over-SSH works whenever SSH does |

## Non-negotiable Principles
- **Documentation is mandatory after ANY change — a session is not complete until this runs, no exceptions:** (1) **update the owning Obsidian vault note(s) per the Documentation Handoff Map — this is a required step, not optional, even for small changes**, (2) update the repo `README.md` if user-facing behavior/config changed, (3) commit and push to **both** `origin` and `github`, (4) **move shipped items to Done in [[Future Improvements]], and actively remove any Done items from the active list once they're verified complete — the Future Improvements file is a living roadmap, not a historical record; clean Done sections keep it useful.** The pattern: mark item "Done" when shipped, move to Done section at end of file, verify the feature works on live instance, then delete the entry from the Done section entirely (or archive to a separate "Archive" section if historical tracking is needed for that quarter).
- **Confirm before anything disruptive or irreversible**: port/volume/network changes; rebuilds that could interrupt active use; anything touching the external `postgres` container; committing or printing secrets anywhere; capability/security-boundary changes to the container (e.g. `cap_add`).
- **Schema changes are additive only** — `_migrate()` per-table pending dicts (`ADD COLUMN IF NOT EXISTS`); new tables via `create_all`. Never `DROP`/destructive `ALTER` on the live DB.
- **Scoring formula/default-weight changes and any loosening of auto-action thresholds require explicit user confirmation** (auto-resolve threshold, LLM blend weight, match title/number weights, per-library profiles when built).
- **Integrations follow `BaseIntegration`** — registration is exactly three places: `_get_client()` + `INTEGRATION_NAMES` in `api/v1/integrations.py`, `_seed_integrations()` in `main.py`. All HTTP via `httpx.AsyncClient(follow_redirects=True)`.
- **LLM assist stays optional and fail-soft** — every consumer works with no LLM configured; single candidates only, never bulk data; strip `<think>` blocks; the LLM is never the sole source of truth for any action.
- **Auth changes need explicit confirmation** and must never risk lockout (LAN bypass defaults protect this); `/api/v1/system/health` stays auth-exempt (Docker healthcheck).
- Don't hammer the *arr hosts: paginated fetches with caps, configurable poll intervals, 60s minimum loops.
- Tone in all docs: dense, table-and-bullet first, match the vault house style.

## Session Routing

1. **Reality check first**: `ssh docker 'docker ps --filter name=powarr'` + health endpoint; confirm local clone and host repo are in sync (`git log` both). Report drift before proceeding.
2. Match the request to a module below; open only that module's vault note(s).
3. Multi-module requests: handle each portion under its own module's rules.
4. Nothing fits → Extension Protocol (new module), not a force-fit.
5. Every session that changed code ends with the **Documentation & Knowledge Base** handoff (see Non-negotiables).

## Module Registry

| Module | Trigger | Owns / Key notes |
|---|---|---|
| Core Backend & Data Model | Routes, models, migrations, scorer, startup, background tasks | `backend/app/` core; [[Architecture]], [[Scoring System]] (+ [[Scoring System — Pre-v0.30 Backup]]). New columns → `_migrate()` same change. Cleanup scorer v0.30.0: series-level watch, sqrt size curve, `watch_half_life_days`, per-library `scoring_profiles` overlays (cleanup only). **v0.50.0**: `GET /media/stats`'s candidate count now applies `cleanup.excluded_libraries` (previously didn't, unlike `GET /media` — caused a live "stats show candidates, page shows nothing" bug); `services/digest.py` weekly-summary rewrite with per-section toggles (`NotificationSettings.digest_include_*`) and two new sections backed by new `ArtistAddLog` table (`models/artist_add_log.py`, written by both `artist_discovery.add_to_lidarr()`/`add_related_artist()`) and `SmartPlaylist.plex_created_at` (set once when `plex_playlist_id` is first assigned) — see [[Scoring System]]. **v0.52.0 (LIB-05)**: opt-in `cleanup.protect_seeding_torrents` hides deletion candidates whose file lives inside an actively-seeding torrent — new `MediaItem.seeding_protected` column, refreshed during Plex sync via `refresh_seeding_protection()` (`services/plex_sync.py`), path-matched (no torrent-hash column on MediaItem) against new `get_seeding_paths()` on both download-client integrations; fail-soft (any unreachable client aborts the refresh, same rule as orphan cleanup). Also closed a pre-existing gap: `GET /media/stats` never filtered on `protected`/`watch_protected` at all — fixed alongside `seeding_protected` so all three protect flags are consistent between the stats tile and `GET /media`. **v0.56.0 (LIB-02)**: Sonarr episode deletes get an explicit policy choice (episode files only / unmonitor season / unmonitor series / remove from Sonarr) instead of always the series-wide default — `deleter.EPISODE_DELETE_MODES`, `deleter._handle_sonarr_episode()`, `MediaItem.pending_delete_mode`, new `SonarrIntegration.get_episode_files()`/`delete_episode_file()`/`set_season_monitored()`; LIB-01's preview modal surfaces a mode selector for Sonarr-episode-only selections. **v0.57.0 (LIB-03)**: `services/duplicate_finder.py::find_duplicate_groups()` groups MediaItem rows (movie/show/artist/album only) by normalized title into duplicate groups, `has_size_signal` flags groups where every member's `file_size` is 0 (show/artist container rows — the real files live on child episodes/tracks) so the caller doesn't present an arbitrary tie-break as a confident recommendation; new read-only `GET /media/duplicates`. **v0.81.0 (LIB-06)**: `services/library_health.py::compute_library_health()` + read-only `GET /media/health` — KPI aggregation (per-type footprint, *arr link coverage for movie/episode/track, duplicate load via LIB-03's grouper, AD-21 thumbnail coverage, open import backlog, protect-flag shielding) from **synced local tables only** (no live *arr calls) and **no composite health score by design** (score formulas are confirmation-gated). See [[Scoring System]], [[Architecture]]. |
| Integrations | Any external service client (Plex, Tautulli, 4× *arr, Seerr, qBittorrent, Transmission, Last.fm, Qdrant, MusicBrainz, Deezer, Bazarr, YouTube, Spotify, future) | `integrations/`; [[Integrations]], [[Adding a New Integration]]. 3-touchpoint rule. Download clients also register in `DOWNLOAD_CLIENT_NAMES` (v0.6.0); qBittorrent auths via username/password + session cookie. `lastfm` (v0.39.0) is read-only, query-param auth (api_key + username), configured via a dedicated `LastFmCard` since the generic `IntegrationCard` doesn't support the username+api_key combo. `qdrant` (v0.34.0, centralized v0.40.0) is AppSetting-backed (key `qdrant`, `services/qdrant_config.py`), not an `Integration` row — single shared connection for Smart Playlists + Artist Discovery, `GET/PUT /integrations/qdrant/settings` + `POST /integrations/qdrant/test` + `POST /integrations/qdrant/full-sync` (v0.45.0, manual-only, wraps `artist_discovery.run_differential_sync()`). `musicbrainz` (v0.40.0) is a plain rate-limited public-API client with no settings UI at all — used only by Artist Discovery enrichment; `deezer` (v0.41.0) follows the same plain-public-client precedent (artist pictures only, enrichment image fallback). `bazarr` (v0.77.0, INT-01) is read-only, used only by the on-demand `GET /media/{id}/subtitle-warning` — deliberately kept out of the pure/network-free `build_deletion_preview()`. `youtube` (v0.78.0, AD-18) is a public Data API v3 client, API key only, no OAuth; `spotify` (v0.78.0, AD-18) uses Client Credentials flow (app-level token via Basic auth on Client ID/Secret, reusing the username+api_key card precedent) — both feed `services/artist_preview.py::get_preview()` for the listen-before-add preview (**v0.79.0**: no longer click-only — availability checked per card scroll-into-view, in-process cached; enable checkboxes labeled "Search YouTube/Spotify for artist previews", default off, Spotify never queried while unchecked). Frontend: reached via **Settings → Integrations** since v0.38.0 (was its own top-level nav item / `pages/Integrations/`, kept in place and rendered embedded). |
| Frontend / UI | Pages, components, UX | `frontend/src/`; only Tailwind classes defined in `tailwind.config.js` (`bg-surface*`, `bg-brand*`, `text-brand-light`); view-layout prefs → localStorage. **v0.38.0 navigation/IA redesign**: left icon rail (`components/IconRail.tsx`, driven by `lib/navConfig.tsx`'s `AREAS` list) replaces the always-expanded sidebar; contextual sub-tab row (`components/AreaTabs.tsx`) for areas with >1 screen; `components/Breadcrumb.tsx` above the content pane; new pages need an entry in `AREAS` (or `LOGS_AREA`) plus a `<Route>` in `App.tsx`, not a manual nav array edit. Mobile hamburger drawer mirrors the same areas/sub-screens two-level. Responsive single-column grids/scrollable tables below `sm:` (v0.25.0); `BotState.tsx` ten-variant animated bot replaces `AnimatedBot.tsx` for all LLM-busy indicators (v0.25.0); tab/filter/sort view state persists per-browser via `lib/usePersistedState.ts` (v0.27.0); row-click multi-select + search/platform filters + `PlatformIcon.tsx` Lucide *arr badges on Match Review triage tables (v0.28.0); animated skeleton loaders replace plain "Loading…" on Overview/Match Review (Skeleton / SkeletonGrid / SkeletonTable components, v0.35.6 — UX-03); **Settings → Music category (v0.40.0)**: `pages/Settings/MusicSettings.tsx` introduces the first internal-tab pattern within a single Settings category (Artist Discovery / Playlists sub-tabs) — prior categories only ever stacked full-width sections, this is a new idiom to reuse for any future multi-config-surface category. **API timestamps (v0.41.1)**: backend datetimes arrive as suffix-less naive UTC — always parse via `parseApiDate()` / render via `fmtDate`/`fmtRelative` from `lib/api.ts`, never raw `new Date(iso)`. **`components/ArtistCard.tsx` (v0.48.0)**: shared avatar/name/bio/genre-chip card body, factored out of Artist Discovery's `CandidateCard`; takes a caller-supplied `subtitle` string and an `actions` node slot so pages with different action semantics (accept/reject vs add/owned-badge) can reuse the same visual shell — reuse this instead of duplicating card markup for any future artist-list surface. **v0.78.0**: `ArtistCard` gained a `preview` slot (full-width below bio) for AD-18's on-demand listen-before-add button. **v0.81.0**: new **Library → Health** KPI page (LIB-06 — tiles link to the actionable page); **UX-05** keyboard triage on Match Review (j/k move a ring-highlighted focus, x select, e expand, y two-step accept, n reject, Esc back out; keys ignored in inputs/with modifiers) — reuse this listener pattern for any future list-page shortcuts. **v0.84.0 (UI polish/a11y pass)**: `index.css` now carries a global `:focus-visible` brand-light outline (keyboard focus ring on every control — the a11y baseline the j/k/x shortcuts needed) — put focus styling there, not per-element; add an `aria-label` to any new icon-only button; use the `Skeleton`/`SkeletonGrid`/`SkeletonTable` loaders, never bare "Loading…" text; `components/Breadcrumb.tsx` is a clickable `Area › Screen` trail; Settings renders a category-switcher pill row and redirects unknown `/settings/:category` to the grid; use the `brand`/`brand-light` tokens for active/primary states, not raw `purple-###`. Deferred (own follow-ups): **FE-01** split the ~2.4k-line `Settings/index.tsx`, **FE-02** normalize route padding. |
| Security | Auth, secrets, exposed surface, CVEs, validation | `services/auth.py`, `api/v1/auth.py`, middleware; [[Security]]. TOTP/LAN-bypass live since v0.3.0; secret-masking API hardening (SEC-01, v0.23.0); Authentik SSO / forward-auth trust + hardened XFF trusted-proxy policy (SEC-02, v0.24.0); Fernet at-rest for integration secrets via `POWARR_FERNET_KEY` (`services/secret_box.py`, SEC-03, v0.34.0); **SEC-A-01 (v0.49.1, user-confirmed)** `auth.default_config()["enabled"]` → `True` for fresh installs only (field has existed since v0.3.0, no safe migration signal for existing rows — see [[Security]] for why). Frontend: **Settings → Security** category since v0.38.0 |
| Docker & Deployment | Dockerfile, compose, env, lifecycle, backup/restore | [[Docker & Deployment]]. Compose file is gitignored — edit on host, never commit. `postgresql-client-16` (PGDG apt repo) added to the image for scheduled `pg_dump` backups (v0.26.0). **v0.72.0 (OPS-02)**: `services/settings_export.py` — config-as-code JSON export/import of `AppSetting` + non-secret `Integration` metadata, separate from the DB backup; `_redact()`/`_unredact_merge()` scrub any credential-shaped field by name across every settings blob (a real Qdrant API key leak was found and fixed via live verification before shipping — see [[Docker & Deployment]] for the finding). **v0.78.1**: `py-spy` live-debugging precedent — install via `pip3 install py-spy` inside a running container, needs a temporary `cap_add: SYS_PTRACE` on the compose service (explicit user approval required, remove after diagnosis). |
| Failed Import Detection & Matching | Stuck imports, queue triage, confidence, auto-resolve, triage table, episode/anime/pack matching, recent-downloads browse | `services/import_matcher.py`, `services/recent_downloads.py`, `services/root_cause.py`, `api/v1/imports.py`, FailedImport model; [[Failed Import Matching]]. Independent of deletion flow. Core: multi-variable episode/pack scorer + deterministic rationale (v0.5.0–v0.5.1), orphan cleanup with positive-confirmation (v0.6.x), import push via `push_import_command()` — **never** POST `/manualimport` directly, **never** seriesId on the manualimport GET (One Piece incident guard, see [[Future Improvements]]), library-folder guard. `APP_FIELDS` maps each app to its id-key/library-fetch-method/title-key (`seriesId`/`get_series`, `movieId`/`get_movies`, `albumId`/`get_albums`, `bookId`/`get_books`) — the shared lookup table `services/recent_downloads.py` also reuses. IA split (v0.38.0): `ImportQueue.tsx` (detection + Scan Now) vs `MatchReview.tsx` (scoring/triage/accept-reject/LLM). Recent: **v0.58.0 (FI-06)** `root_cause.py::classify_root_cause()` — plain-language root-cause tag + suggested action, `root_cause_code`/`_label`/`_action` model properties (same pattern as `match_rationale`/`pack`), Match Review filter dropdown + badge; **v0.59.0 (FI-08)** opt-in `anime_absolute_pack_coverage` (default False) — absolute-episode-range pack coverage for anime batch releases; **v0.60.0 (FI-09)** `recent_downloads.py::list_recent_downloads()` — browse/search recent grabs across all enabled apps; **v0.61.0 (FI-10)** `malformed_audit.py::run_malformed_import_audit()` — nightly (opt-in, off by default) re-check of settled Sonarr pack grabs for incomplete on-disk coverage; new `MalformedImportFlag` table; **v0.69.0 (LLM-09)** `junk_strip_rules` — user-authored ordered regex rules on top of the built-in `strip_release_junk()`, applied before any scoring/LLM sees the title; **v0.70.0 (AN-01)** `import_funnel.py::compute_import_funnel()` — per-`source_app` funnel (suggested → accepted/auto → verified) + failure-reason breakdown reusing the `trackedDownloadState` already snapshotted into `raw_metadata` at row creation (no new schema); `GET /imports/funnel?days=`; Overview gained an Import Funnel card. Full version-by-version history in [[Failed Import Matching]] and [[Future Improvements]]'s Complete table. |
| Smart Playlists | Qdrant genre → Plex playlist review queue with scheduling | `integrations/qdrant.py` (this module's own usage stays read-only), `services/playlist_generator.py`, `api/v1/playlists.py`, `models/smart_playlist.py`, `frontend/src/pages/Music/Playlists.tsx` (settings at Settings → Music → Playlists); [[Smart Playlists]]. Genre playlists from the shared `music_affinity_space` Qdrant collection, with scheduling, blacklist-only artist eligibility (SP-01, no per-artist approval queue), and approve-to-Plex/auto-update. Never writes Qdrant from this module's own code; never adopts existing Plex playlists. Recent: **v0.63.0 (SP-12)** named playlist templates — `_artists_for_template()` unions several genres into one playlist, new `is_template` column; **v0.64.0 (SP-13)** `prune_stale_tracks()` removes tracks whose artist is now blacklisted or whose `plex_key` left the synced Plex library, opt-in default False; **v0.65.0 (SP-09)** related-artist generation axis — `_artists_for_related_seed()`; `mood_tags`/`era` axes remain blocked (confirmed live: never populated anywhere in the pipeline), tracked separately as **SP-15**. **v0.73.0 (SP-11)**: `generate_candidates()` seeds a `used_names` set from every existing playlist title, threaded through all three new-playlist sources in one run; `_playlist_title()` avoid-lists the LLM, hard-checks the result with one retry, falls back to a deterministic unique name — confirmed live that the problem was real (37 real playlist titles already included exact duplicates from independent LLM naming calls with no cross-call awareness). **v0.74.0 (SP-16, user-reported)**: fixed `approve_playlist()` always resolving zero artists (and thus syncing zero tracks) for template/related-seed playlists, since it looked artists up by `genre_tag` alone — that field is a template/seed name, not a real genre, for those two playlist kinds; new dispatch on `is_template` fixes it. Also added a Clear Suggestions bulk-delete button. **v0.78.1 (live incident)**: `_add_artist_tracks_to_db()` re-queried and fully re-materialized the entire tracks table (101k+ rows) on every single artist, called once per artist per genre/template/related-seed group — froze the shared event loop for minutes on every scheduled generation run once the library was large enough. Fixed with a `_tracks_by_artist(db)` index built once per `generate_candidates()` run instead of once per artist. See [[Smart Playlists]] for the full incident writeup and how it was diagnosed (live `py-spy` stack dump). **v0.78.2**: scheduler-level due-ness gate — `_scheduled_playlist_generation` never read `schedule_interval_hours` before; a full generation ran on every 5-minute maintenance tick. Full version-by-version history in [[Smart Playlists]] and [[Future Improvements]]'s Complete table. |
| Artist Discovery | Last.fm/Ollama/Qdrant/Lidarr/MusicBrainz/Deezer artist-discovery pipeline (native port of the n8n Music Curator) | `integrations/lastfm.py`, `integrations/musicbrainz.py`, `integrations/deezer.py`, `services/embeddings.py`, `services/wikipedia.py`, `services/artist_enrichment.py`, `services/artist_discovery.py`, `services/artist_thumbnails.py`, `services/qdrant_config.py`, `api/v1/artist_discovery.py`, `models/artist_discovery.py` (`DiscoveredArtist`, `ArtistDiscoveryRun`), `models/artist_thumbnail.py` (`ArtistThumbnail`), `frontend/src/pages/Music/ArtistDiscovery.tsx`/`RelatedArtists.tsx` (settings at Settings → Music → Artist Discovery); [[Artist Discovery]]. Writes to the same `music_affinity_space` Qdrant collection Smart Playlists reads (soft-delete semantics — never deletes a point, only flips flags). Pipeline: Last.fm top-artists ingest → Ollama embed (`all-minilm`, standalone connection, never reuses Local LLM Assist) → Qdrant taste-centroid similarity search (**two lanes since v0.62.0 — AD-17**: all-time most-played + recently-listened, see below) + related-artist graph expansion → pending review queue (best-match-first, AD-11) → Accept resolves via Lidarr lookup and adds to Lidarr. `auto_promote` (off by default) lets graph candidates crossing the connection threshold skip the queue. Independent optional schedules for the discovery cycle and a separate differential sync. Each candidate gets one-time image/bio/years-active enrichment at creation (Lidarr → MusicBrainz → Wikidata/Wikipedia → Deezer fallback chain). Recent: **v0.48.0 (AD-02)** tray integration + standalone Related Artists search page (no Qdrant writes); **v0.49.x (AD-14/15/16)** Related Artists multi-source Plex badges + hide-owned toggle + pagination; **v0.62.0 (AD-17)** `compute_recent_taste_centroid()` — second discovery lane from recently-listened artists (reuses AD-07's key resolution), run alongside the existing all-time centroid via a shared `_run_centroid_lane()` helper, `source="centroid_recent"` tag, opt-in `recent_taste_lane_enabled` (default True, purely additive). Mood-tag-sliced lanes (AD-17's other example) tracked separately as AD-19. **v0.74.0 (AD-20, user-reported)**: Related Artists search box gained a debounced name-completion typeahead (`GET /related/search-names`, Last.fm `artist.search` — distinct endpoint from `artist.getsimilar`), thumbnail + genres, bio deliberately omitted. **v0.78.0 (AD-18)**: `services/artist_preview.py::get_preview()` — on-demand listen-before-add preview via new read-only `youtube`/`spotify` integrations, `GET /artist-discovery/preview?artist=`, surfaced via `ArtistCard`'s new `preview` slot on Discovery + Related Artists cards; **v0.79.0 rework (user-reported)**: the Play button only renders when a preview is confirmed — viewport-lazy availability check (IntersectionObserver) + in-process cache in `artist_preview.py` (24h hits / 1h misses, enabled-source-fingerprint key); AD-18's original click-only contract retired by user request. **v0.78.2**: differential sync skips no-op Qdrant payload writes (was rewriting all ~285 points hourly; run message now reports `(M unchanged, skipped)` — near-zero synced at steady state is correct). **v0.80.0 (AD-21, user-reported)**: `services/artist_thumbnails.py` + `artist_thumbnails` table (`models/artist_thumbnail.py`) — persistent thumbnail-URL cache for every library artist (Lidarr `remoteUrl` poster → Deezer name-search fallback; **http(s)-only** — Lidarr sometimes reports container-local `/config/MediaCover/...` paths as `remoteUrl`, caught live), daily zero-config scheduler refresh (`last_artist_thumb_refresh`) that deletes rows when an artist leaves the library and aborts wholesale if Lidarr is unreachable; overlaid on the AD-20 typeahead via `thumbnails_for()`, and `_lastfm_image()` now rejects Last.fm's placeholder star. **v0.83.0 (AD-22, user request)**: Recently Added section — read-only `GET /recent-adds` (`list_recent_adds()`: ArtistAddLog newest-first, DiscoveredArtist enrichment join with mbid-then-name fallback, AD-21 thumbnail backfill), rendered via the shared `ArtistCard` shell with `whySuggested()`'s wording reused. Full version-by-version history in [[Artist Discovery]] and [[Future Improvements]]'s Complete table. |
| Local LLM Assist | LLM connection/behavior, prompts, verbosity, on-demand runs, rationale, pack matching | `services/llm_assist.py`, `services/llm_match_log.py`; [[Local LLM Assist]]. Optional, fail-soft, blend 0.3 default. Single structured review call (`review_match`) since v0.5.0; per-file pack matching since v0.13.0; scheduled backlog scanning (v0.26.0); circuit breaker + per-task model overrides (v0.27.0); agree-default/disagree-only-with-contradiction scaffold (v0.30.0); checklist-style small-model guidance + code-enforced `enforce_music_evidence` (v0.36.x); live match model `qwen2.5:7b` on crucible (`10.1.1.4`). **v0.41.0 (LLM-LOG-01)**: `llm_match_log` table logs every real match-review reply (prompt hash, `SCAFFOLD_VERSION`, inputs, App-check flags, raw reply, parse_ok, verdict, `enforced`, latency), ground-truth `resolution` backfilled from the closed FailedImport row, 90-day/5k-row retention, CSV export. **v0.66.0 (LLM-06)**: `compute_accuracy_stats()` pre-aggregates that log into an in-app dashboard (Imports → LLM Accuracy) — parse_ok/agree/enforcement rates + latency by source_app/model/scaffold_version, plus an outcome-agreement metric that deliberately excludes `orphaned` resolutions (file-availability failures, not match-quality failures — a real bug caught via live testing before shipping, see [[Local LLM Assist]] for the numbers). **v0.67.0 (LLM-07)**: independent "risky delete" second opinion on Cleanup deletion candidates — third per-task slot (`second_opinion_enabled`/`_model`) reusing `explain_deletion(verbosity="minimal")`; `item_summary()` now names protection flags explicitly; off by default, advisory only, never blocks a deletion. **v0.68.0 (LLM-08)**: `LlmPolicies` — per-`source_app` match/model/blend-weight overlays (`by_app`) + per-Plex-library explain/model overlays (`by_library`), two separate dicts since `FailedImport`/`MediaItem` share no common key; empty by default, resolved via `_match_policy()`/`_explain_policy()`. Frontend: **Settings → LLM Assist** category since v0.38.0; `BotState` badge next to the Match Review header as an "LLM assist active" indicator. Full version-by-version history in [[Local LLM Assist]] and [[Future Improvements]]'s Complete table. |
| Active Processes / Task Tracking | Background-operation progress tracking, the corner tray | `services/tasks.py`, `api/v1/tasks.py`, `frontend/src/context/TaskContext.tsx`, `frontend/src/components/ActiveProcessesTray.tsx`; [[Active Processes Tray]]. In-memory only, no DB. Events ride the existing `/api/v1/imports/events` bus (`import_matcher.publish()`), not a dedicated endpoint. v0.22.0; LLM task card renders `BotState` `thinking`/`complete`/`error` per status (v0.25.0); `import_batch` kind for async Accept Selected / Process N (v0.28.0); v0.33.0 coalesce same-kind work via `bump_total` (Accept + LLM queue), stack different kinds with slide-up animation, per-item Pushing…; `artist_discovery` kind (v0.48.0, AD-02) — `Compass` icon, indeterminate progress (no natural item count for this pipeline, same as `llm_run`), tray-tracked only for the user-triggered `POST /run`, not the scheduler's own background cycle; `related_search` kind (v0.50.1) — `Users` icon, **determinate** current/total progress (Last.fm candidate count known upfront, unlike the discovery cycle), tray-tracked for `GET /artist-discovery/related` via `search_related_artists_tracked()` |
| Documentation & Knowledge Base | After any code change; explicit docs requests | Vault notes + repo README + this Master Prompt (and its `CLAUDE.md` mirror — keep both in sync) |

## Documentation Handoff Map

| Changed area | Update |
|---|---|
| Backend core / data model | [[Architecture]] (+ [[Scoring System]] if scoring) |
| Integrations | [[Integrations]] (+ [[Adding a New Integration]] if the pattern changed) |
| Frontend | [[Architecture]] directory map (+ [[Powarr Overview]] Pages table if pages changed) |
| Security | [[Security]] |
| Docker/deploy/backup | [[Docker & Deployment]] |
| Failed imports | [[Failed Import Matching]] |
| LLM assist | [[Local LLM Assist]] |
| Smart Playlists / Artist Discovery (Music area) | [[Smart Playlists]] / [[Artist Discovery]] |
| Task tracking / tray | [[Active Processes Tray]] |
| Anything user-facing | Repo `README.md` |
| Every session | [[Future Improvements]] Done section; version bump (`main.py` + `App.tsx` footer) for feature batches; push `origin` + `github` |
| This prompt itself | Keep [[Master Prompt]] and repo `CLAUDE.md` identical |

## Backlog

Work open rows in the **To-Do table** in [[Future Improvements]] (priority then category, i.e. High → Medium → Low, table order within a tier) unless directed otherwise. [[Future Improvements]] is **parse-oriented** — open work lives only in the To-Do table; shipped work lives only in Complete. Its formatting constraints, which this file must stay in sync with:

- **To-Do table schema**: `| ID | Priority | Category | Item | Details | Requested |`. Every row needs an ID before it's actionable.
- **ID convention**: a short category prefix + zero-padded number, unique and permanent once assigned (`LIB-02`, `FI-07`, `AD-14`, `SP-09`, `LLM-06`, `AN-01`, `INT-01`, `OPS-01`, `NOTIF-01`, `SEC-A-01`, `AUTH-01`, `UX-04`, `SCAL-03`, `TEL-01`, `RES-02`, `CONTROL-01`, `MOD-01`, etc.) — never reuse or renumber an ID, even after the row ships or is declined, since old session notes and code comments reference it permanently.
- **Ship a row** → move it into **Complete** at the top of that section (`| **vX.Y.Z** — date | Category | **ID** description... |`) with version/date, then delete the corresponding To-Do row. Full ship-and-document mechanics are in Non-negotiable Principles item 1.
- **Complete is meant to stay pruned, not grow forever** — Non-negotiable Principles item 1 calls for archiving/removing verified-complete entries once they're no longer useful as a reference, not accumulating every release indefinitely. Housekeeping pass done 2026-07-17 (v0.81.0 session): Complete now holds the recent releases (v0.74.0+) and everything older lives **verbatim in an Archive section** at the bottom of [[Future Improvements]] — module notes' "full version-by-version history" references resolve there. Keep that split going forward: move aging Complete rows to Archive rather than deleting them.
- **Feature Ideas & Innovation Opportunities** (bottom section) holds not-yet-triaged strategic ideas — never parse this section for open work. **Promotion workflow**: when an idea from here is accepted for real work, give it a To-Do row + ID and mark it `*Promoted:* see **ID** in To-Do` (or delete it) in place — don't leave a promoted idea duplicated in both sections.
- **Declined-do-not-re-suggest list** lives at the top of the file (currently: `blocklist-on-reject`) — check it before proposing anything that resembles a previously-declined idea, even under new wording.
- Items flagged for confirmation (blend weight, auto-resolve thresholds, auth-by-default, or any row whose Details column says "user decision"/"needs confirmation"/conditional-on-an-event-that-hasn't-happened) get **user sign-off before implementation**, not just before merge — don't build first and ask after.

## Extension Protocol

New capability → new module section + registry row here (and in `CLAUDE.md`); don't stretch an existing module. Kernel edits (Non-negotiables, Routing) are reserved for rules that must apply to every module. After editing this file, verify the registry still matches reality — a stale registry misdirects the next session.
