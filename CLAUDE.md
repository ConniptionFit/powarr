# Powarr — Master Prompt (LLM IDE Agent)

> **This is the authoritative agent context for Powarr.** Use this whole file as a prefix, together with the linked vault notes, before any future additions to the app. Supersedes [[LLM Context]] (legacy stub). Canonical copy: `Powarr/Master Prompt.md` in the Obsidian vault — keep this mirror identical. Last updated: 2026-07-12 (v0.63.0 — **SP-12** named playlist templates, next open Medium-priority To-Do item, agent-selected (AD-18 skipped — needs YouTube/Spotify API credentials not available in this environment): a template is a playlist generated from the union of several genres (e.g. "Workout" = Rock+Electronic+Hip-Hop), narrower slice of open SP-09. **Real finding from researching against the live Plex server first** (same discipline as SP-02): the item's other ask — weighting by Sonic Analysis "energy/valence" — isn't implementable as literally described; probed a real track's full metadata and confirmed Plex's public API only exposes Sonic Analysis via nearest-neighbor similarity search (already used by `sonic_similarity_enabled`) plus per-stream loudness/gain/peak (mastering loudness, not a mood score) — no queryable raw energy/valence value exists. Documented rather than building something that wouldn't deliver what was described. New opt-in `playlist_templates: dict[str, list[str]]` (empty by default, taxonomy left to the user like SP-14's `genre_aliases`); `_artists_for_template()` + `_upsert_playlist_group()` (extracted shared create/sync logic) in `playlist_generator.py`; new `SmartPlaylist.is_template` column. 7 new backend unit tests, 574 total pass; deployed + verified live. See [[Smart Playlists]]. Prior: v0.62.0 **AD-17** recent-taste discovery lane — second centroid seeded from recently-listened artists alongside the all-time one, `source="centroid_recent"`, opt-in `recent_taste_lane_enabled` default True. AD-17's mood-tag-lane example not done, tracked as AD-19. Prior: v0.61.0 **FI-10** nightly malformed-import audit. Prior: v0.60.0 **FI-09** Recent Downloads browse/force-reimport page. **Process note for future sessions**: a v0.56.0 same-session false alarm (deployed/reverted twice) traced to this container's normal ~3 minute post-restart Artist Discovery sync burst, which makes the healthcheck legitimately report `unhealthy` for a few minutes; **don't judge a fresh deploy as broken before waiting ~3-4 minutes for this container specifically.** Full detail on all versions above, plus older entries (v0.39.0–v0.58.0), are covered in [[Future Improvements]]'s Complete table — kept terse here per the Non-negotiable Principles' own pruning intent.

## Identity & Purpose

You are an LLM IDE agent responsible for reviewing, maintaining, and extending **Powarr** — a self-hosted FastAPI/React media-cleanup and failed-import-triage tool for the Plex + *arr stack. A good session: check reality against docs before trusting either, make a scoped change through exactly one capability module, verify it on the live container, and leave **both the Obsidian vault and GitHub** accurately reflecting what changed.

## Project Facts

| Field | Value |
|---|---|
| Version | v0.63.0 — **2026-07-12**. Full changelog for this version is in the header note above; older entries live in [[Future Improvements]]'s Complete table (parse-oriented, always current) rather than growing this row forever — recent highlights: v0.62.0 AD-17 recent-taste lane, v0.61.0 FI-10 malformed-import audit, v0.60.0 FI-09 recent-downloads/reimport, v0.59.0 FI-08 anime pack coverage, v0.58.0 FI-06 root-cause classifier, v0.57.0 LIB-03 duplicate hunter, v0.56.0 LIB-02 Sonarr episode delete modes, v0.55.0 SP-14 genre fuzzy-merge, v0.54.0 LIB-04 watch-progress protect, v0.53.0 LIB-01 deletion preview, v0.52.0 LIB-05 seeding protection, v0.51.0 FI-07 auto-gate inspector, v0.50.x Deletion Suggestions bug fix + digest sections + tray progress, v0.49.x SEC-A-01/AD-14/AD-15/AD-16, v0.48.0 Artist Discovery UX + Related Artists, v0.44.0-v0.47.x auto-import gate + SP-02 + AD-10/11, v0.39.0-v0.42.x Artist Discovery + Smart Playlists shipped. |
| Container | `powarr`, port `7979`, Docker host `10.1.1.2` (`ssh docker`, key auth) |
| Source of truth | Host repo `/mnt/ServerFiles/Docker/composeFiles/powarr` (= build context) |
| Local working clone | `~/Projects/powarr` on the Mac — **edit here**. Confirmed 2026-07-08: this is the current FastAPI/React app (`backend/`, `frontend/`, `CLAUDE.md`); `origin` = host repo, `github` = public mirror. The legacy Node.js iteration was archived to `~/Projects/powarr-legacy-node` (reference only, not deployed). |
| Remotes | `origin` = host repo (push-to-deploy: `receive.denyCurrentBranch=updateInstead`); `github` = public [ConniptionFit/powarr](https://github.com/ConniptionFit/powarr) (`gh` CLI authed) — renamed from the private `powarr-v2` 2026-07-08, confirmed live |
| Deploy | commit → `git push origin` → `ssh docker 'cd /mnt/ServerFiles/Docker/composeFiles/powarr && docker compose up --build -d'` → verify → `git push github` |
| Stack | Python 3.12 + FastAPI + SQLAlchemy; React 18 + TS + Tailwind; single multi-stage image; HEALTHCHECK on `/api/v1/system/health` |
| Database | PostgreSQL container `postgres` on `postgres_net` (db/user `powarr`); SQLite fallback |
| API | `/api/v1/*` (breaking changes → `/api/v2/`); docs at `/api/docs` |
| Tests | `docker exec powarr python -m unittest discover -s app/tests` (must pass before handoff). Local pre-deploy iteration: `cd ~/Projects/powarr/backend && POWARR_DATA_DIR=<writable> uv run --python 3.12 --with pydantic --with pydantic-settings --with sqlalchemy --with httpx --with fastapi python -m unittest discover -s app/tests` |
| Secrets | `docker-compose.yml` is **gitignored** (DB password inline — never commit); API keys/auth secrets live in DB rows |
| Old iteration | `~/Projects/powarr-legacy-node` is the archived legacy Node.js Powarr (reference only, not deployed, not the source of truth for anything). |
| Trap | `/Volumes/ServerFiles` SMB mount and the `10.1.1.x` LAN link flap — if unreachable, it's the Mac's network, not the server; git-over-SSH works whenever SSH does |

## Non-negotiable Principles
- **Documentation is mandatory after ANY change — a session is not complete until this runs, no exceptions:** (1) **update the owning Obsidian vault note(s) per the Documentation Handoff Map — this is a required step, not optional, even for small changes**, (2) update the repo `README.md` if user-facing behavior/config changed, (3) commit and push to **both** `origin` and `github`, (4) **move shipped items to Done in [[Future Improvements]], and actively remove any Done items from the active list once they're verified complete — the Future Improvements file is a living roadmap, not a historical record; clean Done sections keep it useful.** The pattern: mark item "Done" when shipped, move to Done section at end of file, verify the feature works on live instance, then delete the entry from the Done section entirely (or archive to a separate "Archive" section if historical tracking is needed for that quarter).
- **Confirm before anything disruptive or irreversible**: port/volume/network changes; rebuilds that could interrupt active use; anything touching the external `postgres` container; committing or printing secrets anywhere.
- **Schema changes are additive only** — `_migrate()` per-table pending dicts (`ADD COLUMN IF NOT EXISTS`); new tables via `create_all`. Never `DROP`/destructive `ALTER` on the live DB.
- **Scoring formula/default-weight changes and any loosening of auto-action thresholds require explicit user confirmation** (auto-resolve threshold, LLM blend weight, match title/number weights, per-library profiles when built).
- **Integrations follow `BaseIntegration`** — registration is exactly three places: `_get_client()` + `INTEGRATION_NAMES` in `api/v1/integrations.py`, `_seed_integrations()` in `main.py`. All HTTP via `httpx.AsyncClient(follow_redirects=True)`.
- **LLM assist stays optional and fail-soft** — every consumer works with no LLM configured; single candidates only, never bulk data; strip `<think>` blocks; the LLM is never the sole source of truth for any action.
- **Auth changes need explicit confirmation** and must never risk lockout (LAN bypass defaults protect this); `/api/v1/system/health` stays auth-exempt (Docker healthcheck).
- Don't hammer the *arr hosts: paginated fetches with caps, configurable poll intervals, 60s minimum loops.
- Tone in all docs: dense, table-and-bullet first, match the vault house style.

## Session Routing

1. **Reality check first**: `ssh docker 'docker ps --filter name=powarr'` + health endpoint; confirm local clone and host repo are in sync (`git log` both). Report drift before proceeding.
2. Match the request to a module below; open only that module's vault note(s).
3. Multi-module requests: handle each portion under its own module's rules.
4. Nothing fits → Extension Protocol (new module), not a force-fit.
5. Every session that changed code ends with the **Documentation & Knowledge Base** handoff (see Non-negotiables).

## Module Registry

| Module | Trigger | Owns / Key notes |
|---|---|---|
| Core Backend & Data Model | Routes, models, migrations, scorer, startup, background tasks | `backend/app/` core; [[Architecture]], [[Scoring System]] (+ [[Scoring System — Pre-v0.30 Backup]]). New columns → `_migrate()` same change. Cleanup scorer v0.30.0: series-level watch, sqrt size curve, `watch_half_life_days`, per-library `scoring_profiles` overlays (cleanup only). **v0.50.0**: `GET /media/stats`'s candidate count now applies `cleanup.excluded_libraries` (previously didn't, unlike `GET /media` — caused a live "stats show candidates, page shows nothing" bug); `services/digest.py` weekly-summary rewrite with per-section toggles (`NotificationSettings.digest_include_*`) and two new sections backed by new `ArtistAddLog` table (`models/artist_add_log.py`, written by both `artist_discovery.add_to_lidarr()`/`add_related_artist()`) and `SmartPlaylist.plex_created_at` (set once when `plex_playlist_id` is first assigned) — see [[Scoring System]]. **v0.52.0 (LIB-05)**: opt-in `cleanup.protect_seeding_torrents` hides deletion candidates whose file lives inside an actively-seeding torrent — new `MediaItem.seeding_protected` column, refreshed during Plex sync via `refresh_seeding_protection()` (`services/plex_sync.py`), path-matched (no torrent-hash column on MediaItem) against new `get_seeding_paths()` on both download-client integrations; fail-soft (any unreachable client aborts the refresh, same rule as orphan cleanup). Also closed a pre-existing gap: `GET /media/stats` never filtered on `protected`/`watch_protected` at all — fixed alongside `seeding_protected` so all three protect flags are consistent between the stats tile and `GET /media`. **v0.56.0 (LIB-02)**: Sonarr episode deletes get an explicit policy choice (episode files only / unmonitor season / unmonitor series / remove from Sonarr) instead of always the series-wide default — `deleter.EPISODE_DELETE_MODES`, `deleter._handle_sonarr_episode()`, `MediaItem.pending_delete_mode`, new `SonarrIntegration.get_episode_files()`/`delete_episode_file()`/`set_season_monitored()`; LIB-01's preview modal surfaces a mode selector for Sonarr-episode-only selections. **v0.57.0 (LIB-03)**: `services/duplicate_finder.py::find_duplicate_groups()` groups MediaItem rows (movie/show/artist/album only) by normalized title into duplicate groups, `has_size_signal` flags groups where every member's `file_size` is 0 (show/artist container rows — the real files live on child episodes/tracks) so the caller doesn't present an arbitrary tie-break as a confident recommendation; new read-only `GET /media/duplicates`. See [[Scoring System]]. |
| Integrations | Any external service client (Plex, Tautulli, 4× *arr, Seerr, qBittorrent, Transmission, Last.fm, Qdrant, MusicBrainz, Deezer, future) | `integrations/`; [[Integrations]], [[Adding a New Integration]]. 3-touchpoint rule. Download clients also register in `DOWNLOAD_CLIENT_NAMES` (v0.6.0); qBittorrent auths via username/password + session cookie. `lastfm` (v0.39.0) is read-only, query-param auth (api_key + username), configured via a dedicated `LastFmCard` since the generic `IntegrationCard` doesn't support the username+api_key combo. `qdrant` (v0.34.0, centralized v0.40.0) is AppSetting-backed (key `qdrant`, `services/qdrant_config.py`), not an `Integration` row — single shared connection for Smart Playlists + Artist Discovery, `GET/PUT /integrations/qdrant/settings` + `POST /integrations/qdrant/test` + `POST /integrations/qdrant/full-sync` (v0.45.0, manual-only, wraps `artist_discovery.run_differential_sync()`). `musicbrainz` (v0.40.0) is a plain rate-limited public-API client with no settings UI at all — used only by Artist Discovery enrichment; `deezer` (v0.41.0) follows the same plain-public-client precedent (artist pictures only, enrichment image fallback). Frontend: reached via **Settings → Integrations** since v0.38.0 (was its own top-level nav item / `pages/Integrations/`, kept in place and rendered embedded) |
| Frontend / UI | Pages, components, UX | `frontend/src/`; only Tailwind classes defined in `tailwind.config.js` (`bg-surface*`, `bg-brand*`, `text-brand-light`); view-layout prefs → localStorage. **v0.38.0 navigation/IA redesign**: left icon rail (`components/IconRail.tsx`, driven by `lib/navConfig.tsx`'s `AREAS` list) replaces the always-expanded sidebar; contextual sub-tab row (`components/AreaTabs.tsx`) for areas with >1 screen; `components/Breadcrumb.tsx` above the content pane; new pages need an entry in `AREAS` (or `LOGS_AREA`) plus a `<Route>` in `App.tsx`, not a manual nav array edit. Mobile hamburger drawer mirrors the same areas/sub-screens two-level. Responsive single-column grids/scrollable tables below `sm:` (v0.25.0); `BotState.tsx` ten-variant animated bot replaces `AnimatedBot.tsx` for all LLM-busy indicators (v0.25.0); tab/filter/sort view state persists per-browser via `lib/usePersistedState.ts` (v0.27.0); row-click multi-select + search/platform filters + `PlatformIcon.tsx` Lucide *arr badges on Match Review triage tables (v0.28.0); animated skeleton loaders replace plain "Loading…" on Overview/Match Review (Skeleton / SkeletonGrid / SkeletonTable components, v0.35.6 — UX-03); **Settings → Music category (v0.40.0)**: `pages/Settings/MusicSettings.tsx` introduces the first internal-tab pattern within a single Settings category (Artist Discovery / Playlists sub-tabs) — prior categories only ever stacked full-width sections, this is a new idiom to reuse for any future multi-config-surface category. **API timestamps (v0.41.1)**: backend datetimes arrive as suffix-less naive UTC — always parse via `parseApiDate()` / render via `fmtDate`/`fmtRelative` from `lib/api.ts`, never raw `new Date(iso)`. **`components/ArtistCard.tsx` (v0.48.0)**: shared avatar/name/bio/genre-chip card body, factored out of Artist Discovery's `CandidateCard`; takes a caller-supplied `subtitle` string and an `actions` node slot so pages with different action semantics (accept/reject vs add/owned-badge) can reuse the same visual shell — reuse this instead of duplicating card markup for any future artist-list surface |
| Security | Auth, secrets, exposed surface, CVEs, validation | `services/auth.py`, `api/v1/auth.py`, middleware; [[Security]]. TOTP/LAN-bypass live since v0.3.0; secret-masking API hardening (SEC-01, v0.23.0); Authentik SSO / forward-auth trust + hardened XFF trusted-proxy policy (SEC-02, v0.24.0); Fernet at-rest for integration secrets via `POWARR_FERNET_KEY` (`services/secret_box.py`, SEC-03, v0.34.0); **SEC-A-01 (v0.49.1, user-confirmed)** `auth.default_config()["enabled"]` → `True` for fresh installs only (field has existed since v0.3.0, no safe migration signal for existing rows — see [[Security]] for why). Frontend: **Settings → Security** category since v0.38.0 |
| Docker & Deployment | Dockerfile, compose, env, lifecycle, backup/restore | [[Docker & Deployment]]. Compose file is gitignored — edit on host, never commit. `postgresql-client-16` (PGDG apt repo) added to the image for scheduled `pg_dump` backups (v0.26.0) |
| Failed Import Detection & Matching | Stuck imports, queue triage, confidence, auto-resolve, triage table, episode/anime/pack matching, recent-downloads browse | `services/import_matcher.py`, `services/recent_downloads.py`, `services/root_cause.py`, `api/v1/imports.py`, FailedImport model; [[Failed Import Matching]]. Independent of deletion flow. Core: multi-variable episode/pack scorer + deterministic rationale (v0.5.0–v0.5.1), orphan cleanup with positive-confirmation (v0.6.x), import push via `push_import_command()` — **never** POST `/manualimport` directly, **never** seriesId on the manualimport GET (One Piece incident guard, see [[Future Improvements]]), library-folder guard. `APP_FIELDS` maps each app to its id-key/library-fetch-method/title-key (`seriesId`/`get_series`, `movieId`/`get_movies`, `albumId`/`get_albums`, `bookId`/`get_books`) — the shared lookup table `services/recent_downloads.py` also reuses. IA split (v0.38.0): `ImportQueue.tsx` (detection + Scan Now) vs `MatchReview.tsx` (scoring/triage/accept-reject/LLM). Recent: **v0.44.0** dual-signal auto-import gate; **v0.47.1 (FI-02)** pack-coverage double-count rationale caveat; **v0.51.0 (FI-07)** `describe_auto_gate()` "why not auto-import?" inspector; **v0.58.0 (FI-06)** `root_cause.py::classify_root_cause()` — plain-language root-cause tag + suggested action, `root_cause_code`/`_label`/`_action` model properties (same pattern as `match_rationale`/`pack`), Match Review filter dropdown + badge; **v0.59.0 (FI-08)** opt-in `anime_absolute_pack_coverage` (default False) — absolute-episode-range pack coverage for anime batch releases; **v0.60.0 (FI-09)** `recent_downloads.py::list_recent_downloads()` — browse/search recent grabs across all enabled apps (concurrent per-app fetch via `asyncio.gather`), `POST /imports/recent-downloads/reimport` reuses `push_import_command()` directly; **v0.61.0 (FI-10)** `malformed_audit.py::run_malformed_import_audit()` — nightly (opt-in, off by default) re-check of settled Sonarr pack grabs for incomplete on-disk coverage via a new shared `pack_scope_episodes()` helper (also used by `_pack_coverage()`) and Sonarr's `hasFile` flag; new `MalformedImportFlag` table, `GET/POST /imports/malformed-audit*`, one ntfy notification per audit run. Full version-by-version history in [[Failed Import Matching]] and [[Future Improvements]]'s Complete table. |
| Smart Playlists | Qdrant genre → Plex playlist review queue with scheduling | `integrations/qdrant.py` (this module's own usage stays read-only), `services/playlist_generator.py`, `api/v1/playlists.py`, `models/smart_playlist.py`, `frontend/src/pages/Music/Playlists.tsx` (settings at Settings → Music → Playlists); [[Smart Playlists]]. Genre playlists from the shared `music_affinity_space` Qdrant collection, with scheduling, blacklist-only artist eligibility (SP-01, no per-artist approval queue), and approve-to-Plex/auto-update. Never writes Qdrant from this module's own code; never adopts existing Plex playlists. Recent: **v0.47.0 (SP-02)** `sonic_similarity_enabled` biases per-artist track selection toward Plex Sonic-Analysis-nearest tracks — pure re-ranking, not a generation criterion; **v0.55.0 (SP-14)** genre fuzzy-merge (`merge_genre_groups`, user-curated `genre_aliases`); **v0.63.0 (SP-12)** named playlist templates — `_artists_for_template()` unions several genres into one playlist (opt-in `playlist_templates` map, empty by default), `_upsert_playlist_group()` shared create/sync helper, new `is_template` column; confirmed live against Plex that raw Sonic Analysis energy/valence values aren't exposed by the API (only similarity search), so SP-12/SP-02 both work via re-ranking/genre-union, never direct mood scoring. Open SP-09 (multi-factor generation beyond genre/template-union) remains the broader ask. Full version-by-version history in [[Smart Playlists]] and [[Future Improvements]]'s Complete table. |
| Artist Discovery | Last.fm/Ollama/Qdrant/Lidarr/MusicBrainz/Deezer artist-discovery pipeline (native port of the n8n Music Curator) | `integrations/lastfm.py`, `integrations/musicbrainz.py`, `integrations/deezer.py`, `services/embeddings.py`, `services/wikipedia.py`, `services/artist_enrichment.py`, `services/artist_discovery.py`, `services/qdrant_config.py`, `api/v1/artist_discovery.py`, `models/artist_discovery.py` (`DiscoveredArtist`, `ArtistDiscoveryRun`), `frontend/src/pages/Music/ArtistDiscovery.tsx`/`RelatedArtists.tsx` (settings at Settings → Music → Artist Discovery); [[Artist Discovery]]. Writes to the same `music_affinity_space` Qdrant collection Smart Playlists reads (soft-delete semantics — never deletes a point, only flips flags). Pipeline: Last.fm top-artists ingest → Ollama embed (`all-minilm`, standalone connection, never reuses Local LLM Assist) → Qdrant taste-centroid similarity search (**two lanes since v0.62.0 — AD-17**: all-time most-played + recently-listened, see below) + related-artist graph expansion → pending review queue (best-match-first, AD-11) → Accept resolves via Lidarr lookup and adds to Lidarr. `auto_promote` (off by default) lets graph candidates crossing the connection threshold skip the queue. Independent optional schedules for the discovery cycle and a separate differential sync. Each candidate gets one-time image/bio/years-active enrichment at creation (Lidarr → MusicBrainz → Wikidata/Wikipedia → Deezer fallback chain). Recent: **v0.48.0 (AD-02)** tray integration + standalone Related Artists search page (no Qdrant writes); **v0.49.x (AD-14/15/16)** Related Artists multi-source Plex badges + hide-owned toggle + pagination; **v0.62.0 (AD-17)** `compute_recent_taste_centroid()` — second discovery lane from recently-listened artists (reuses AD-07's key resolution), run alongside the existing all-time centroid via a shared `_run_centroid_lane()` helper, `source="centroid_recent"` tag, opt-in `recent_taste_lane_enabled` (default True, purely additive). Mood-tag-sliced lanes (AD-17's other example) tracked separately as AD-19. Full version-by-version history in [[Artist Discovery]] and [[Future Improvements]]'s Complete table. |
| Local LLM Assist | LLM connection/behavior, prompts, verbosity, on-demand runs, rationale, pack matching | `services/llm_assist.py`; [[Local LLM Assist]]. Optional, fail-soft, blend 0.3 default. Single structured review call (`review_match`) since v0.5.0; per-file pack matching (`review_pack_files`) since v0.13.0 with triggered-series context prioritization; year-mismatch guard (v0.19.0); judging-guidance fix (ignore quality/uploader tags, consider title translations) + forced-verbose on-demand runs + `llm_agrees` structured field (prefix removed) + Markdown reply format + clickable prompt placeholders (v0.20.0); queued on-demand runs (no more 409) + concise `PACK_MATCH_TYPES` pack-match taxonomy + single-object salvage fix (v0.21.0); `BotState` ten-variant animated busy indicator (v0.25.0, replaces the old `AnimatedBot` CSS spinner); scheduled backlog scanning — quiet-hours window or always-on trickle, on top of the on-demand runs (`AppSetting` key `llm_schedule`, `scheduler.py::_scheduled_llm_run`, v0.26.0); in-memory call stats + circuit breaker (auto-pause after N consecutive failures, `GET /settings/llm/stats` + reset, config applied at startup and settings save) + per-task toggles/model overrides (`task_enabled()`/`model_for()` on `OllamaSettings`) + Explain Visible batch button on Deletion Suggestions (v0.27.0); v0.30.0: agree-default / disagree-only-with-contradiction, `forbid_thinking` + `compact_det_summary` (defaults on), fixed Markdown-capable JSON envelope (Settings format picker removed), pack prompt uses pack+folder+files, Pack Files Settings tab; v0.32.0: per-`source_app` `_APP_MATCH_GUIDANCE` (Lidarr music-only, no anime rules); v0.36.0–v0.36.2: sonarr/lidarr guidance rewritten checklist-style for small models, deterministic `music_match_evidence()` App-check context line at both review sites and code-enforced in the negative direction (`enforce_music_evidence` — an LLM agree cannot stand when a containment check fails), adjustment sign tied to verdict, English-only reasons; live match model `qwen2.5:7b` on crucible (`10.1.1.4`, 680M/ROCm, 16 GiB VRAM carve-out); **v0.41.0 (LLM-LOG-01)**: `llm_match_log` table logs every real match-review reply at both review sites (prompt hash + `SCAFFOLD_VERSION` constant, inputs, App-check flags, raw reply, parse_ok, verdict/adjustment, `enforced`, latency — `review_match(capture=)` out-param, `services/llm_match_log.py` record/backfill/prune, terminal FailedImport status backfilled as ground truth, 90-day/5k-row retention, CSV export `GET /imports/llm-log/export.csv`); `suggest_playlist_name()` (SP-04) added to the fail-soft contract. Frontend: **Settings → LLM Assist** category since v0.38.0 (Ollama connection card moved there from the old Integrations page); small `BotState` badge now sits next to the Match Review header title as an "LLM assist active" indicator |
| Active Processes / Task Tracking | Background-operation progress tracking, the corner tray | `services/tasks.py`, `api/v1/tasks.py`, `frontend/src/context/TaskContext.tsx`, `frontend/src/components/ActiveProcessesTray.tsx`; [[Active Processes Tray]]. In-memory only, no DB. Events ride the existing `/api/v1/imports/events` bus (`import_matcher.publish()`), not a dedicated endpoint. v0.22.0; LLM task card renders `BotState` `thinking`/`complete`/`error` per status (v0.25.0); `import_batch` kind for async Accept Selected / Process N (v0.28.0); v0.33.0 coalesce same-kind work via `bump_total` (Accept + LLM queue), stack different kinds with slide-up animation, per-item Pushing…; `artist_discovery` kind (v0.48.0, AD-02) — `Compass` icon, indeterminate progress (no natural item count for this pipeline, same as `llm_run`), tray-tracked only for the user-triggered `POST /run`, not the scheduler's own background cycle; `related_search` kind (v0.50.1) — `Users` icon, **determinate** current/total progress (Last.fm candidate count known upfront, unlike the discovery cycle), tray-tracked for `GET /artist-discovery/related` via `search_related_artists_tracked()` |
| Documentation & Knowledge Base | After any code change; explicit docs requests | Vault notes + repo README + this Master Prompt (and its `CLAUDE.md` mirror — keep both in sync) |

## Documentation Handoff Map

| Changed area | Update |
|---|---|
| Backend core / data model | [[Architecture]] (+ [[Scoring System]] if scoring) |
| Integrations | [[Integrations]] (+ [[Adding a New Integration]] if the pattern changed) |
| Frontend | [[Architecture]] directory map (+ [[Powarr Overview]] Pages table if pages changed) |
| Security | [[Security]] |
| Docker/deploy/backup | [[Docker & Deployment]] |
| Failed imports | [[Failed Import Matching]] |
| LLM assist | [[Local LLM Assist]] |
| Smart Playlists / Artist Discovery (Music area) | [[Smart Playlists]] / [[Artist Discovery]] |
| Task tracking / tray | [[Active Processes Tray]] |
| Anything user-facing | Repo `README.md` |
| Every session | [[Future Improvements]] Done section; version bump (`main.py` + `App.tsx` footer) for feature batches; push `origin` + `github` |
| This prompt itself | Keep [[Master Prompt]] and repo `CLAUDE.md` identical |

## Backlog

Work open rows in the **To-Do table** in [[Future Improvements]] (priority then category, i.e. High → Medium → Low, table order within a tier) unless directed otherwise. [[Future Improvements]] is **parse-oriented** — open work lives only in the To-Do table; shipped work lives only in Complete. Its formatting constraints, which this file must stay in sync with:

- **To-Do table schema**: `| ID | Priority | Category | Item | Details | Requested |`. Every row needs an ID before it's actionable.
- **ID convention**: a short category prefix + zero-padded number, unique and permanent once assigned (`LIB-02`, `FI-07`, `AD-14`, `SP-09`, `LLM-06`, `AN-01`, `INT-01`, `OPS-01`, `NOTIF-01`, `SEC-A-01`, `AUTH-01`, `UX-04`, `SCAL-03`, `TEL-01`, `RES-02`, `CONTROL-01`, `MOD-01`, etc.) — never reuse or renumber an ID, even after the row ships or is declined, since old session notes and code comments reference it permanently.
- **Ship a row** → move it into **Complete** at the top of that section (`| **vX.Y.Z** — date | Category | **ID** description... |`) with version/date, then delete the corresponding To-Do row. Full ship-and-document mechanics are in Non-negotiable Principles item 1.
- **Complete is meant to stay pruned, not grow forever** — Non-negotiable Principles item 1 calls for archiving/removing verified-complete entries once they're no longer useful as a reference, not accumulating every release indefinitely. In practice this file has drifted from that (Complete currently runs the full history back to v0.22.0) — a housekeeping pass to prune/archive older Complete entries is itself fair backlog work, not urgent.
- **Feature Ideas & Innovation Opportunities** (bottom section) holds not-yet-triaged strategic ideas — never parse this section for open work. **Promotion workflow**: when an idea from here is accepted for real work, give it a To-Do row + ID and mark it `*Promoted:* see **ID** in To-Do` (or delete it) in place — don't leave a promoted idea duplicated in both sections.
- **Declined-do-not-re-suggest list** lives at the top of the file (currently: `blocklist-on-reject`) — check it before proposing anything that resembles a previously-declined idea, even under new wording.
- Items flagged for confirmation (blend weight, auto-resolve thresholds, auth-by-default, or any row whose Details column says "user decision"/"needs confirmation"/conditional-on-an-event-that-hasn't-happened) get **user sign-off before implementation**, not just before merge — don't build first and ask after.

## Extension Protocol

New capability → new module section + registry row here (and in `CLAUDE.md`); don't stretch an existing module. Kernel edits (Non-negotiables, Routing) are reserved for rules that must apply to every module. After editing this file, verify the registry still matches reality — a stale registry misdirects the next session.
