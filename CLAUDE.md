# Powarr — Master Prompt (LLM IDE Agent)

> **This is the authoritative agent context for Powarr.** Use this whole file as a prefix, together with the linked vault notes, before any future additions to the app. Supersedes [[LLM Context]] (legacy stub). Canonical copy: `Powarr/Master Prompt.md` in the Obsidian vault — keep this mirror identical. Last updated: 2026-07-12 (v0.51.0 — **FI-07**, next open To-Do item, agent-selected: Match Review rows gained a **"Why not auto-import?"** inspector — an expandable panel (next to Change Match on `suggested`/`resolve_failed` rows, both card and table views) showing exactly which leg(s) of the v0.44.0 dual-signal auto-import gate passed or failed against the configured thresholds, and a plain-language reason when the row isn't auto-importing yet (wrong status, no match, threshold miss per-mode). Backed by a new pure `describe_auto_gate()` helper (`services/auto_eligible.py`) exposed at `GET /imports/{id}/auto-gate` — deliberately mirrors `is_auto_eligible()`'s exact logic (cross-checked by a parametrized agreement test) so the inspector can never show a verdict that disagrees with what the scanner actually does. Read-only: no schema change, no rescore/LLM call triggered. 9 new backend unit tests, 450 total pass; `npm run build` clean; verified live against a real `orphaned` row on the production instance (correctly reported "Status 'orphaned' isn't eligible"). See [[Failed Import Matching]]. Prior: v0.50.1 — user report: searching Related Artists gave no feedback that anything was happening, since per-candidate enrichment (Lidarr/MusicBrainz/Wikipedia/Deezer round-trips) can take 15+ seconds and the page only showed a static "Searching…" button label. Fixed by mirroring `run_discovery()`'s tray-wrapper pattern: new `search_related_artists_tracked()` creates a `related_search` task (new `Users`-icon kind) before the Last.fm call, sets `total` to the candidate count once known, increments `current` per candidate as `_enrich_candidate()` finishes — the first **determinate** progress bar among the Related Artists/Artist Discovery task kinds (`artist_discovery` stays indeterminate — no natural upfront item count for that pipeline). `GET /artist-discovery/related` now calls the tracked wrapper. 2 new unit tests, 441 total pass; `npm run build` clean. See [[Active Processes Tray]], [[Artist Discovery]]. Prior: v0.50.0 — user-reported bug fix + digest feature request: (1) **excluded_libraries stats bug** — `GET /media/stats`'s `candidates_above_threshold` never applied `cleanup.excluded_libraries`, while `GET /media` (what Deletion Suggestions actually renders) did; the live instance had all three real libraries (`Movies`/`TV Shows`/`Music`) excluded, so the dashboard showed 133 candidates while the page rendered nothing — fixed `get_stats()` to apply the same filter, cleared the live setting via the Settings API (see [[Scoring System]]). (2) **Weekly digest overhaul** — new per-section toggles (`digest_include_imports`/`_artists`/`_playlists`/`_cleanup`, all default on) plus two new sections: artists added (new `ArtistAddLog` table, one row per successful Lidarr add from either Discovery-accept or Related-Artists, written inside both flows' shared call sites — distinguishes a genuine add from Lidarr's "already there" 400-response case via a new `already_existed` field on `_add_artist_to_lidarr()`'s return) and playlists created (new `SmartPlaylist.plex_created_at` column, set once when `plex_playlist_id` is first assigned, distinct from `created_at`/`updated_at`). Settings → Notifications gained four checkboxes under Weekly digest summary. 15 new unit tests, 439 total pass; `npm run build` clean. See [[Scoring System]], [[Architecture]]. Prior v0.42.0–v0.49.1 (2026-07-10/12): SEC-A-01 auth-by-default; AD-14 Related Artists multi-source Plex badges (confirmed live); documentation-drift catch-up session (vault re-sync, Future Improvements reconciliation); AD-15/AD-16 hide-owned toggle + pagination; AD-02 tray + Related Artists page shipped; SCAL-03 fetch-cache dedup; FI-02 pack-coverage caveat; SP-02 sonic-similarity bias (+ fixed a severe pre-existing bug where no Smart Playlist track had ever reached Plex since v0.42.1) and a leaked-credential remediation; AD-11 best-match ordering; AD-10 already-owned exclusion; AD-07/08 dual thresholds + thumbnail retention; SP-01/05–08 blacklist-only playlists + approve-to-Plex. Full detail in [[Future Improvements]]'s Complete table.

## Identity & Purpose

You are an LLM IDE agent responsible for reviewing, maintaining, and extending **Powarr** — a self-hosted FastAPI/React media-cleanup and failed-import-triage tool for the Plex + *arr stack. A good session: check reality against docs before trusting either, make a scoped change through exactly one capability module, verify it on the live container, and leave **both the Obsidian vault and GitHub** accurately reflecting what changed.

## Project Facts

| Field | Value |
|---|---|
| Version | v0.51.0 — **2026-07-12**. Full changelog for this version is in the header note above; older entries live in [[Future Improvements]]'s Complete table (parse-oriented, always current) rather than growing this row forever — recent highlights: v0.50.1 Related Artists tray progress, v0.50.0 Deletion Suggestions bug fix + digest sections, v0.49.x SEC-A-01/AD-14/AD-15/AD-16, v0.48.0 Artist Discovery UX + Related Artists, v0.44.0-v0.47.x auto-import gate + SP-02 + AD-10/11, v0.39.0-v0.42.x Artist Discovery + Smart Playlists shipped. |
| Container | `powarr`, port `7979`, Docker host `10.1.1.2` (`ssh docker`, key auth) |
| Source of truth | Host repo `/mnt/ServerFiles/Docker/composeFiles/powarr` (= build context) |
| Local working clone | `~/Projects/powarr` on the Mac — **edit here**. Confirmed 2026-07-08: this is the current FastAPI/React app (`backend/`, `frontend/`, `CLAUDE.md`); `origin` = host repo, `github` = public mirror. The legacy Node.js iteration was archived to `~/Projects/powarr-legacy-node` (reference only, not deployed). |
| Remotes | `origin` = host repo (push-to-deploy: `receive.denyCurrentBranch=updateInstead`); `github` = public [ConniptionFit/powarr](https://github.com/ConniptionFit/powarr) (`gh` CLI authed) — renamed from the private `powarr-v2` 2026-07-08, confirmed live |
| Deploy | commit → `git push origin` → `ssh docker 'cd /mnt/ServerFiles/Docker/composeFiles/powarr && docker compose up --build -d'` → verify → `git push github` |
| Stack | Python 3.12 + FastAPI + SQLAlchemy; React 18 + TS + Tailwind; single multi-stage image; HEALTHCHECK on `/api/v1/system/health` |
| Database | PostgreSQL container `postgres` on `postgres_net` (db/user `powarr`); SQLite fallback |
| API | `/api/v1/*` (breaking changes → `/api/v2/`); docs at `/api/docs` |
| Tests | `docker exec powarr python -m unittest discover -s app/tests` (must pass before handoff). Local pre-deploy iteration: `cd ~/Projects/powarr/backend && POWARR_DATA_DIR=<writable> uv run --python 3.12 --with pydantic --with pydantic-settings --with sqlalchemy --with httpx --with fastapi python -m unittest discover -s app/tests` |
| Secrets | `docker-compose.yml` is **gitignored** (DB password inline — never commit); API keys/auth secrets live in DB rows |
| Old iteration | `~/Projects/powarr-legacy-node` is the archived legacy Node.js Powarr (reference only, not deployed, not the source of truth for anything). |
| Trap | `/Volumes/ServerFiles` SMB mount and the `10.1.1.x` LAN link flap — if unreachable, it's the Mac's network, not the server; git-over-SSH works whenever SSH does |

## Non-negotiable Principles
- **Documentation is mandatory after ANY change — a session is not complete until this runs, no exceptions:** (1) **update the owning Obsidian vault note(s) per the Documentation Handoff Map — this is a required step, not optional, even for small changes**, (2) update the repo `README.md` if user-facing behavior/config changed, (3) commit and push to **both** `origin` and `github`, (4) **move shipped items to Done in [[Future Improvements]], and actively remove any Done items from the active list once they're verified complete — the Future Improvements file is a living roadmap, not a historical record; clean Done sections keep it useful.** The pattern: mark item "Done" when shipped, move to Done section at end of file, verify the feature works on live instance, then delete the entry from the Done section entirely (or archive to a separate "Archive" section if historical tracking is needed for that quarter).
- **Confirm before anything disruptive or irreversible**: port/volume/network changes; rebuilds that could interrupt active use; anything touching the external `postgres` container; committing or printing secrets anywhere.
- **Schema changes are additive only** — `_migrate()` per-table pending dicts (`ADD COLUMN IF NOT EXISTS`); new tables via `create_all`. Never `DROP`/destructive `ALTER` on the live DB.
- **Scoring formula/default-weight changes and any loosening of auto-action thresholds require explicit user confirmation** (auto-resolve threshold, LLM blend weight, match title/number weights, per-library profiles when built).
- **Integrations follow `BaseIntegration`** — registration is exactly three places: `_get_client()` + `INTEGRATION_NAMES` in `api/v1/integrations.py`, `_seed_integrations()` in `main.py`. All HTTP via `httpx.AsyncClient(follow_redirects=True)`.
- **LLM assist stays optional and fail-soft** — every consumer works with no LLM configured; single candidates only, never bulk data; strip `<think>` blocks; the LLM is never the sole source of truth for any action.
- **Auth changes need explicit confirmation** and must never risk lockout (LAN bypass defaults protect this); `/api/v1/system/health` stays auth-exempt (Docker healthcheck).
- Don't hammer the *arr hosts: paginated fetches with caps, configurable poll intervals, 60s minimum loops.
- Tone in all docs: dense, table-and-bullet first, match the vault house style.

## Session Routing

1. **Reality check first**: `ssh docker 'docker ps --filter name=powarr'` + health endpoint; confirm local clone and host repo are in sync (`git log` both). Report drift before proceeding.
2. Match the request to a module below; open only that module's vault note(s).
3. Multi-module requests: handle each portion under its own module's rules.
4. Nothing fits → Extension Protocol (new module), not a force-fit.
5. Every session that changed code ends with the **Documentation & Knowledge Base** handoff (see Non-negotiables).

## Module Registry

| Module | Trigger | Owns / Key notes |
|---|---|---|
| Core Backend & Data Model | Routes, models, migrations, scorer, startup, background tasks | `backend/app/` core; [[Architecture]], [[Scoring System]] (+ [[Scoring System — Pre-v0.30 Backup]]). New columns → `_migrate()` same change. Cleanup scorer v0.30.0: series-level watch, sqrt size curve, `watch_half_life_days`, per-library `scoring_profiles` overlays (cleanup only). **v0.50.0**: `GET /media/stats`'s candidate count now applies `cleanup.excluded_libraries` (previously didn't, unlike `GET /media` — caused a live "stats show candidates, page shows nothing" bug); `services/digest.py` weekly-summary rewrite with per-section toggles (`NotificationSettings.digest_include_*`) and two new sections backed by new `ArtistAddLog` table (`models/artist_add_log.py`, written by both `artist_discovery.add_to_lidarr()`/`add_related_artist()`) and `SmartPlaylist.plex_created_at` (set once when `plex_playlist_id` is first assigned) — see [[Scoring System]]. |
| Integrations | Any external service client (Plex, Tautulli, 4× *arr, Seerr, qBittorrent, Transmission, Last.fm, Qdrant, MusicBrainz, Deezer, future) | `integrations/`; [[Integrations]], [[Adding a New Integration]]. 3-touchpoint rule. Download clients also register in `DOWNLOAD_CLIENT_NAMES` (v0.6.0); qBittorrent auths via username/password + session cookie. `lastfm` (v0.39.0) is read-only, query-param auth (api_key + username), configured via a dedicated `LastFmCard` since the generic `IntegrationCard` doesn't support the username+api_key combo. `qdrant` (v0.34.0, centralized v0.40.0) is AppSetting-backed (key `qdrant`, `services/qdrant_config.py`), not an `Integration` row — single shared connection for Smart Playlists + Artist Discovery, `GET/PUT /integrations/qdrant/settings` + `POST /integrations/qdrant/test` + `POST /integrations/qdrant/full-sync` (v0.45.0, manual-only, wraps `artist_discovery.run_differential_sync()`). `musicbrainz` (v0.40.0) is a plain rate-limited public-API client with no settings UI at all — used only by Artist Discovery enrichment; `deezer` (v0.41.0) follows the same plain-public-client precedent (artist pictures only, enrichment image fallback). Frontend: reached via **Settings → Integrations** since v0.38.0 (was its own top-level nav item / `pages/Integrations/`, kept in place and rendered embedded) |
| Frontend / UI | Pages, components, UX | `frontend/src/`; only Tailwind classes defined in `tailwind.config.js` (`bg-surface*`, `bg-brand*`, `text-brand-light`); view-layout prefs → localStorage. **v0.38.0 navigation/IA redesign**: left icon rail (`components/IconRail.tsx`, driven by `lib/navConfig.tsx`'s `AREAS` list) replaces the always-expanded sidebar; contextual sub-tab row (`components/AreaTabs.tsx`) for areas with >1 screen; `components/Breadcrumb.tsx` above the content pane; new pages need an entry in `AREAS` (or `LOGS_AREA`) plus a `<Route>` in `App.tsx`, not a manual nav array edit. Mobile hamburger drawer mirrors the same areas/sub-screens two-level. Responsive single-column grids/scrollable tables below `sm:` (v0.25.0); `BotState.tsx` ten-variant animated bot replaces `AnimatedBot.tsx` for all LLM-busy indicators (v0.25.0); tab/filter/sort view state persists per-browser via `lib/usePersistedState.ts` (v0.27.0); row-click multi-select + search/platform filters + `PlatformIcon.tsx` Lucide *arr badges on Match Review triage tables (v0.28.0); animated skeleton loaders replace plain "Loading…" on Overview/Match Review (Skeleton / SkeletonGrid / SkeletonTable components, v0.35.6 — UX-03); **Settings → Music category (v0.40.0)**: `pages/Settings/MusicSettings.tsx` introduces the first internal-tab pattern within a single Settings category (Artist Discovery / Playlists sub-tabs) — prior categories only ever stacked full-width sections, this is a new idiom to reuse for any future multi-config-surface category. **API timestamps (v0.41.1)**: backend datetimes arrive as suffix-less naive UTC — always parse via `parseApiDate()` / render via `fmtDate`/`fmtRelative` from `lib/api.ts`, never raw `new Date(iso)`. **`components/ArtistCard.tsx` (v0.48.0)**: shared avatar/name/bio/genre-chip card body, factored out of Artist Discovery's `CandidateCard`; takes a caller-supplied `subtitle` string and an `actions` node slot so pages with different action semantics (accept/reject vs add/owned-badge) can reuse the same visual shell — reuse this instead of duplicating card markup for any future artist-list surface |
| Security | Auth, secrets, exposed surface, CVEs, validation | `services/auth.py`, `api/v1/auth.py`, middleware; [[Security]]. TOTP/LAN-bypass live since v0.3.0; secret-masking API hardening (SEC-01, v0.23.0); Authentik SSO / forward-auth trust + hardened XFF trusted-proxy policy (SEC-02, v0.24.0); Fernet at-rest for integration secrets via `POWARR_FERNET_KEY` (`services/secret_box.py`, SEC-03, v0.34.0); **SEC-A-01 (v0.49.1, user-confirmed)** `auth.default_config()["enabled"]` → `True` for fresh installs only (field has existed since v0.3.0, no safe migration signal for existing rows — see [[Security]] for why). Frontend: **Settings → Security** category since v0.38.0 |
| Docker & Deployment | Dockerfile, compose, env, lifecycle, backup/restore | [[Docker & Deployment]]. Compose file is gitignored — edit on host, never commit. `postgresql-client-16` (PGDG apt repo) added to the image for scheduled `pg_dump` backups (v0.26.0) |
| Failed Import Detection & Matching | Stuck imports, queue triage, confidence, auto-resolve, triage table, episode/anime/pack matching | `services/import_matcher.py`, `api/v1/imports.py`, FailedImport model; [[Failed Import Matching]]. Independent of deletion flow. **v0.38.0**: frontend split into `pages/Imports/ImportQueue.tsx` (detection-only list + Scan Now) and `pages/Imports/MatchReview.tsx` (was `pages/Cleanup/FailedImports.tsx` — all matching/scoring/pack-review/accept-reject/LLM logic, now card-per-item by default with a table-view toggle); both share the same `FailedImport` data/status set (no backend change — the split is a frontend view/column split, not a new status). Multi-variable episode scorer + deterministic rationale (v0.5.0), season-pack coverage (v0.5.1), orphan cleanup w/ positive-confirmation rule (v0.6.0), prompt-first orphan confirmation + filesystem presence leg + auto-purge toggle (v0.6.1), import push via ManualImport command — never POST /manualimport, never seriesId on the manualimport GET, library-folder guard (v0.6.2/v0.6.3, see incident in [[Future Improvements]]), triggered-series metadata in LLM context + per-file episode review for season packs (v0.13.0), title-similarity punctuation fix + manual-import paired-episode corroboration (v0.16.0), editable per-file episode mapping via `PUT /imports/{id}/file-mapping` (wired into accept) + quality-downgrade detection/auto-reject (v0.17.0, replaces the removed v0.15.0 pack columns), `BaseIntegration.remove_from_queue()` clearing the *arr's own queue entry on orphan-auto-purge/downgrade-auto-reject, no blocklist (v0.18.0), suspicious file-type detection across all apps (`find_suspicious_files`, user-editable extension list) + auto-reject + optional delete-from-disk via `remove_from_download_clients()` (v0.19.0), ntfy click-to-act Accept/Reject notification links via signed one-time tokens (`services/action_tokens.py`, v0.26.0), Process N Items + async batch accept with tray progress (`services/auto_eligible.py`, `import_batch` task kind, v0.28.0), empty-manualimport / gone downloads → orphaned with warning at accept + each scan (v0.28.1), Servarr NullReference 500 → folder-only `manualimport` retry + `reason: no_files` (`BaseIntegration._fetch_manual_import` / `extract_output_path`, v0.29.0), Lidarr/Readarr music edition strip + partial gap-fill Accept (`partial_import`, skip covered files, Partial badge + green/red file rows, v0.32.0), Lidarr/Readarr **album/book** library matching + LLM rematch on rescore (v0.34.0 — fixes artist-only LLM disagree), `still_in_queue` + reopen API (v0.35.0; filter-bar redesign reverted v0.35.1), recovery operations (RescanSeries, RetagSeries, etc) via `BaseIntegration.run_command()` + `POST /imports/{id}/recover` endpoint (v0.35.5 — OPS-01, recovery after incidents), **v0.44.0** dual-signal auto-import gate (`passes_auto_thresholds` — see Version row) + per-scan backlog drain of gate-passing suggested rows + `purge_dead_queue_entry()` at every files-gone orphan site + scan dedupe extended to resolve_failed/orphaned-still-in-queue, **v0.47.1 (FI-02)** double-segment/anthology pack coverage caveat in `score_pack_match()` — rationale-only, no score change, **v0.47.2 (SCAL-03)** `_verify_resolved()`/match-path share one `get_history()` fetch per app per cycle instead of two, **v0.51.0 (FI-07)** `describe_auto_gate()` (`services/auto_eligible.py`) + `GET /imports/{id}/auto-gate` — read-only per-row breakdown of the dual-signal auto-import gate (algorithm/LLM leg values vs thresholds, `would_auto_import`, plain-language `reasons`), surfaced as a "Why not auto-import?" expander on Match Review `suggested`/`resolve_failed` rows |
| Smart Playlists | Qdrant genre → Plex playlist review queue with scheduling | `integrations/qdrant.py` (this module's own usage stays read-only), `services/playlist_generator.py`, `api/v1/playlists.py`, `models/smart_playlist.py`, `frontend/src/pages/Music/Playlists.tsx` (queue-only as of v0.40.0 — settings moved to Settings → Music → Playlists, `pages/Settings/MusicSettings.tsx`); [[Smart Playlists]]. v0.35.2: scheduling, auto-add, track tracking via SmartPlaylistRun/Track tables; per-playlist overrides. v0.39.0: closed two UI gaps (`excluded_genres`, `auto_create_playlists` fields; per-playlist override editor). v0.40.0: dropped its own `qdrant_url`/`qdrant_api_key`/`collection` settings fields — reads the shared `qdrant_config.client(db)` instead; removed the now-superseded `/smart-playlists/qdrant/test` endpoint. v0.41.0 (SP-04): optional `llm_playlist_names` toggle — new playlists named via `llm_assist.suggest_playlist_name()`, fail-soft to the `Powarr · {genre}` template. v0.42.0/v0.42.1 (SP-01/05–08): approve-to-Plex + auto-update, blacklist-only eligibility replacing the per-artist queue, on-demand LLM naming — see [[Smart Playlists]] for full detail. **v0.47.0 (SP-02)**: optional `sonic_similarity_enabled` biases per-artist track selection toward Plex Sonic-Analysis-nearest tracks (`PlexIntegration.sonically_similar_keys()`), pure re-ranking on top of Qdrant eligibility; also fixed a severe bug (`_add_artist_tracks_to_db()` filtering on a nonexistent `MediaItem.is_monitored_lidarr` column, AttributeError on every call since v0.42.1 — no track had ever actually reached Plex). Never write Qdrant from this module's own code; never adopt existing Plex playlists. Open SP-01 redesign (auto-generation from taste signals, replacing per-artist approval) is **blocked on a design discussion + explicit user confirmation** — do not implement unprompted. |
| Artist Discovery | Last.fm/Ollama/Qdrant/Lidarr/MusicBrainz/Deezer artist-discovery pipeline (native port of the n8n Music Curator) | `integrations/lastfm.py`, `integrations/musicbrainz.py`, `integrations/deezer.py`, `services/embeddings.py`, `services/wikipedia.py`, `services/artist_enrichment.py`, `services/artist_discovery.py`, `services/qdrant_config.py`, `api/v1/artist_discovery.py`, `models/artist_discovery.py` (`DiscoveredArtist`, `ArtistDiscoveryRun`), `frontend/src/pages/Music/ArtistDiscovery.tsx` (queue-only as of v0.40.0 — settings moved to Settings → Music → Artist Discovery); [[Artist Discovery]]. Writes to the same `music_affinity_space` Qdrant collection Smart Playlists reads (shared connection as of v0.40.0, see Integrations row) — soft-delete semantics only (never deletes a point, only flips flags), a different module than Smart Playlists so its own read-only boundary is unaffected. Full pipeline: Last.fm top-artists ingest → Ollama embed (`all-minilm` default, standalone connection since v0.40.0 — never reuses the Local LLM Assist Ollama host) → Qdrant taste-centroid similarity search + related-artist graph expansion (seed connection threshold) → pending review queue → Accept resolves via Lidarr lookup (MBID → normalized-name → first-result fallback) and adds to Lidarr. `auto_promote` setting (off by default, user-confirmed) lets graph candidates crossing the connection threshold skip the queue. Independent optional schedules for the discovery cycle and a separate differential sync (Lidarr/Last.fm state → Qdrant flags). **Artist enrichment (v0.40.0; image chain fixed v0.41.0 — AD-04)**: each candidate gets a one-time `image_url`/`bio`/`years_active` fetch at creation (Lidarr lookup primary; MusicBrainz for active-years + genre/bio fallback; Wikipedia reached via the **wikidata** url-rel's sitelinks since v0.41.0 — direct wikipedia rels are deprecated upstream and were why v0.40.0 art never resolved — plus Wikidata P18 and `integrations/deezer.py` as the final image fallback) — rendered as a thumbnail + clamped bio on the review-queue card; `re_enrich_missing()` backfills incomplete pending rows each cycle + via `POST /candidates/re-enrich`. **v0.41.0 additions**: `seed_artist_names` column (AD-05 — all contributing seeds resolved to names, refreshed as new connections land, plain-language why-suggested card line), `clean_tags()`/`clean_era()` placeholder filtering (AD-06), `GET /runs` + Recent runs table (AD-03). n8n's `music_curator_*` workflows are left running untouched — not migrated/deactivated by this. **v0.44.0**: every full discovery cycle (scheduled or Run Now) starts with `run_differential_sync()` (fail-soft, refreshes `last_artist_discovery_sync`) so centroid/graph always compute on fresh Lidarr/Last.fm play-history state. **v0.46.0 (AD-10)**: `run_differential_sync()` also writes `in_lidarr` (any Lidarr artist, monitored or not) + `in_plex` (real check against locally-synced `MediaItem` tracks) onto every point; `run_centroid_discovery()` filters both out via Qdrant `must_not`; `run_graph_sync()` does a live Lidarr+Plex ownership check (`_lidarr_artist_index()`, `_plex_artist_names()`) before any embed/upsert work, skipping already-owned related artists outright. **v0.46.1 (AD-11)**: `GET /artist-discovery/candidates` sorts best-match-first (`_match_rating_key()` in `api/v1/artist_discovery.py`) instead of newest-first — centroid rows by `similarity_score` desc, graph rows by connection count desc, centroid group always ahead of graph group. **v0.48.0 (AD-02 + Related Artists)**: `run_discovery()` tray-tracked wrapper around `run_full_discovery_cycle()` (`artist_discovery` task kind — see Active Processes row); page buttons narrowed to just Run Discovery Now; stats tile relabeled "Taste model size" + SSE live-refresh. New, independent of the taste-model pipeline: `frontend/src/pages/Music/RelatedArtists.tsx` (`/music/related`) + `search_related_artists()`/`add_related_artist()` (`GET/POST /artist-discovery/related*`) — ad-hoc Last.fm similar-artist search for any artist, enriched the same way candidates are, already-owned flagged not hidden, adds straight to Lidarr via the extracted `_add_artist_to_lidarr()` helper (also now used by the original Accept flow) with **no** Qdrant writes or `discovered_artists` rows. **v0.49.0 (AD-15/AD-16)**: Related Artists gained a hide-already-owned toggle (client-side filter on `already_owned`, checked by default) and real pagination — `search_related_artists()` takes a `limit` param (clamped 1–200, default 50, was hardcoded to 15) passed through to `LastFmIntegration.get_similar_artists()`; since Last.fm's `artist.getsimilar` has no offset param (only a limit, rank-ordered), the frontend's **Load more** button re-queries with `limit += 50` rather than attempting real cursor pagination. **v0.49.1 (AD-14)**: `_augment_with_plex_similarity()` adds Plex Sonically Similar + Plex Similar Artists as second/third badge sources alongside Last.fm (MusicBrainz declined by the user, no native "similar artist" concept) — new `PlexIntegration.find_artist()`/`sonically_similar_artists()`/`related_artists()` (`integrations/plex.py`) merged by normalized name, each result's new `similarity_sources` array rendered as colored badges via `components/ArtistCard.tsx`'s new `sourceBadges` prop. Built without live Plex access (extracting the user's Plex token or `docker cp`+`docker exec`-ing a probe script into the live container were both correctly declined as too invasive for an unprompted write/exec on production) and confirmed live immediately post-deploy instead: a real `GET .../related?artist=Radiohead` returned genuine `plex_sonic`/`plex_similar` badges plus new Plex-only discoveries (Grandaddy, Coldplay, Modest Mouse, etc.) with real Deezer enrichment — both assumed endpoint shapes correct on the first try. **v0.50.1**: `search_related_artists_tracked()` wraps the search in a `related_search` tray task (see Active Processes row) — determinate `current`/`total` progress once the Last.fm candidate count is known, addressing a user report that the page gave no feedback during the 15+ second enrichment phase; `GET /artist-discovery/related` now calls the tracked wrapper. |
| Local LLM Assist | LLM connection/behavior, prompts, verbosity, on-demand runs, rationale, pack matching | `services/llm_assist.py`; [[Local LLM Assist]]. Optional, fail-soft, blend 0.3 default. Single structured review call (`review_match`) since v0.5.0; per-file pack matching (`review_pack_files`) since v0.13.0 with triggered-series context prioritization; year-mismatch guard (v0.19.0); judging-guidance fix (ignore quality/uploader tags, consider title translations) + forced-verbose on-demand runs + `llm_agrees` structured field (prefix removed) + Markdown reply format + clickable prompt placeholders (v0.20.0); queued on-demand runs (no more 409) + concise `PACK_MATCH_TYPES` pack-match taxonomy + single-object salvage fix (v0.21.0); `BotState` ten-variant animated busy indicator (v0.25.0, replaces the old `AnimatedBot` CSS spinner); scheduled backlog scanning — quiet-hours window or always-on trickle, on top of the on-demand runs (`AppSetting` key `llm_schedule`, `scheduler.py::_scheduled_llm_run`, v0.26.0); in-memory call stats + circuit breaker (auto-pause after N consecutive failures, `GET /settings/llm/stats` + reset, config applied at startup and settings save) + per-task toggles/model overrides (`task_enabled()`/`model_for()` on `OllamaSettings`) + Explain Visible batch button on Deletion Suggestions (v0.27.0); v0.30.0: agree-default / disagree-only-with-contradiction, `forbid_thinking` + `compact_det_summary` (defaults on), fixed Markdown-capable JSON envelope (Settings format picker removed), pack prompt uses pack+folder+files, Pack Files Settings tab; v0.32.0: per-`source_app` `_APP_MATCH_GUIDANCE` (Lidarr music-only, no anime rules); v0.36.0–v0.36.2: sonarr/lidarr guidance rewritten checklist-style for small models, deterministic `music_match_evidence()` App-check context line at both review sites and code-enforced in the negative direction (`enforce_music_evidence` — an LLM agree cannot stand when a containment check fails), adjustment sign tied to verdict, English-only reasons; live match model `qwen2.5:7b` on crucible (`10.1.1.4`, 680M/ROCm, 16 GiB VRAM carve-out); **v0.41.0 (LLM-LOG-01)**: `llm_match_log` table logs every real match-review reply at both review sites (prompt hash + `SCAFFOLD_VERSION` constant, inputs, App-check flags, raw reply, parse_ok, verdict/adjustment, `enforced`, latency — `review_match(capture=)` out-param, `services/llm_match_log.py` record/backfill/prune, terminal FailedImport status backfilled as ground truth, 90-day/5k-row retention, CSV export `GET /imports/llm-log/export.csv`); `suggest_playlist_name()` (SP-04) added to the fail-soft contract. Frontend: **Settings → LLM Assist** category since v0.38.0 (Ollama connection card moved there from the old Integrations page); small `BotState` badge now sits next to the Match Review header title as an "LLM assist active" indicator |
| Active Processes / Task Tracking | Background-operation progress tracking, the corner tray | `services/tasks.py`, `api/v1/tasks.py`, `frontend/src/context/TaskContext.tsx`, `frontend/src/components/ActiveProcessesTray.tsx`; [[Active Processes Tray]]. In-memory only, no DB. Events ride the existing `/api/v1/imports/events` bus (`import_matcher.publish()`), not a dedicated endpoint. v0.22.0; LLM task card renders `BotState` `thinking`/`complete`/`error` per status (v0.25.0); `import_batch` kind for async Accept Selected / Process N (v0.28.0); v0.33.0 coalesce same-kind work via `bump_total` (Accept + LLM queue), stack different kinds with slide-up animation, per-item Pushing…; `artist_discovery` kind (v0.48.0, AD-02) — `Compass` icon, indeterminate progress (no natural item count for this pipeline, same as `llm_run`), tray-tracked only for the user-triggered `POST /run`, not the scheduler's own background cycle; `related_search` kind (v0.50.1) — `Users` icon, **determinate** current/total progress (Last.fm candidate count known upfront, unlike the discovery cycle), tray-tracked for `GET /artist-discovery/related` via `search_related_artists_tracked()` |
| Documentation & Knowledge Base | After any code change; explicit docs requests | Vault notes + repo README + this Master Prompt (and its `CLAUDE.md` mirror — keep both in sync) |

## Documentation Handoff Map

| Changed area | Update |
|---|---|
| Backend core / data model | [[Architecture]] (+ [[Scoring System]] if scoring) |
| Integrations | [[Integrations]] (+ [[Adding a New Integration]] if the pattern changed) |
| Frontend | [[Architecture]] directory map (+ [[Powarr Overview]] Pages table if pages changed) |
| Security | [[Security]] |
| Docker/deploy/backup | [[Docker & Deployment]] |
| Failed imports | [[Failed Import Matching]] |
| LLM assist | [[Local LLM Assist]] |
| Smart Playlists / Artist Discovery (Music area) | [[Smart Playlists]] / [[Artist Discovery]] |
| Task tracking / tray | [[Active Processes Tray]] |
| Anything user-facing | Repo `README.md` |
| Every session | [[Future Improvements]] Done section; version bump (`main.py` + `App.tsx` footer) for feature batches; push `origin` + `github` |
| This prompt itself | Keep [[Master Prompt]] and repo `CLAUDE.md` identical |

## Backlog

Work open rows in the **To-Do table** in [[Future Improvements]] (priority then category) unless directed otherwise. Do not re-suggest explicitly declined items (blocklist-on-reject). Items flagged for confirmation (blend weight, auto-resolve thresholds) get user sign-off at implementation time. The Future Improvements file is parse-oriented: open work lives only in To-Do; shipped work in Complete.

## Extension Protocol

New capability → new module section + registry row here (and in `CLAUDE.md`); don't stretch an existing module. Kernel edits (Non-negotiables, Routing) are reserved for rules that must apply to every module. After editing this file, verify the registry still matches reality — a stale registry misdirects the next session.
